/**
 * Auto-generated classifier sheet
 * Generated by: rjit classify
 * Patterns: 665
 * Total occurrences: 5015
 *
 * Review names, adjust as needed, then import at app entry.
 */

import { classifier } from '@reactjit/core';

classifier({
  // 467 occurrences across 164 files
  TextPlain: { type: 'Text', fontSize: 10 },

  // 377 occurrences across 167 files
  TextPlain2: { type: 'Text', fontSize: 9 },

  // 197 occurrences across 110 files
  BoldTextPlain: { type: 'Text', fontSize: 10, style: { fontWeight: 'bold' } },

  // 116 occurrences across 90 files
  Spacer: { type: 'Box', style: { flexGrow: 1 } },

  // 98 occurrences across 60 files
  BoldTextPlain2: { type: 'Text', fontSize: 11, style: { fontWeight: 'bold' } },

  // 86 occurrences across 65 files
  InlineG62s: { type: 'Row', style: { gap: 6, alignItems: 'center' } },

  // 79 occurrences across 57 files
  InlineG8: { type: 'Row', style: { gap: 8, alignItems: 'center' } },

  // 77 occurrences across 34 files
  CaptionPlain: { type: 'Text', size: 9 },

  // 74 occurrences across 57 files
  BoldTextPlain3: { type: 'Text', fontSize: 9, style: { fontWeight: 'bold' } },

  // 71 occurrences across 45 files
  BoldTextPlain4: { type: 'Text', fontSize: 12, style: { fontWeight: 'bold' } },

  // 62 occurrences across 41 files
  InlineG62s2: { type: 'Row', style: { gap: 6, flexWrap: 'wrap' } },

  // 57 occurrences across 37 files
  InlineG8V2: { type: 'Row', style: { alignItems: 'center', gap: 8, flexWrap: 'wrap' } },

  // 54 occurrences across 41 files
  Stack4: { type: 'Col', style: { gap: 4 } },

  // 51 occurrences across 33 files
  BoldTextPlain5: { type: 'Text', style: { fontWeight: 'bold' } },

  // 46 occurrences across 32 files
  InlineG8V3: { type: 'Row', style: { gap: 8, flexWrap: 'wrap' } },

  // 44 occurrences across 29 files
  Stack6: { type: 'Col', style: { gap: 6 } },

  // 43 occurrences across 31 files
  Stack8: { type: 'Col', style: { gap: 8 } },

  // 43 occurrences across 25 files
  TextPlain3: { type: 'Text', fontSize: 11 },

  // 40 occurrences across 26 files
  TextPlain4: { type: 'Text', fontSize: 8 },

  // 36 occurrences across 20 files
  BodyPlain: { type: 'Text', size: 10 },

  // 36 occurrences across 32 files
  InlineG42s: { type: 'Row', style: { gap: 4, alignItems: 'center' } },

  // 35 occurrences across 20 files
  TextPlain5: { type: 'Text', fontSize: 9, style: { fontFamily: 'monospace' } },

  // 34 occurrences across 31 files
  HalfGap23s: { type: 'Col', style: { flexGrow: 1, flexBasis: 0, gap: 2 } },

  // 33 occurrences across 19 files
  BoldTextPlain6: { type: 'Text', fontSize: 13, style: { fontWeight: 'bold' } },

  // 32 occurrences across 29 files
  Stack2: { type: 'Col', style: { gap: 2 } },

  // 30 occurrences across 30 files
  Bare: { type: 'Graph', originTopLeft: true },

  // 29 occurrences across 18 files
  TextPlain6: { type: 'Text', fontSize: 10, style: { fontFamily: 'monospace' } },

  // 28 occurrences across 24 files
  Inline: { type: 'Row', style: { justifyContent: 'space-between', alignItems: 'center' } },

  // 28 occurrences across 17 files
  CaptionPlain2: { type: 'Text', size: 9 },

  // 27 occurrences across 21 files
  Stack10: { type: 'Col', style: { gap: 10 } },

  // 24 occurrences across 16 files
  BodyPlain2: { type: 'Text', size: 10 },

  // 22 occurrences across 8 files
  Pad10: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
      borderWidth: 1,
    } },

  // 21 occurrences across 11 files
  BodyPlain3: { type: 'Text', size: 10, style: { letterSpacing: 1 } },

  // 20 occurrences across 12 files
  Stack8V2: { type: 'Box', style: { gap: 8 } },

  // 20 occurrences across 13 files
  Body11Plain: { type: 'Text', size: 11 },

  // 20 occurrences across 11 files
  TinyPlain: { type: 'Text', size: 8, style: { fontFamily: 'monospace' } },

  // 19 occurrences across 15 files
  InlineG61s: { type: 'Row', style: { gap: 6 } },

  // 19 occurrences across 15 files
  CaptionPlain3: { type: 'Text', size: 9, style: { letterSpacing: 1 } },

  // 19 occurrences across 13 files
  InlineG63s: { type: 'Row', style: { alignItems: 'center', gap: 6, flexWrap: 'wrap' } },

  // 18 occurrences across 16 files
  Box2pW100Pct: { type: 'Col', style: { width: '100%', height: '100%' } },

  // 18 occurrences across 7 files
  Stack14: { type: 'Col', style: { gap: 14 } },

  // 18 occurrences across 14 files
  InlineG8V4: { type: 'Row', style: { gap: 8 } },

  // 18 occurrences across 17 files
  BoldTextPlain7: { type: 'Text', fontSize: 14, style: { fontWeight: 'bold' } },

  // 18 occurrences across 17 files
  InlineG42s2: { type: 'Row', style: { flexWrap: 'wrap', gap: 4 } },

  // 17 occurrences across 16 files
  Dot83s: { type: 'Box', style: { width: 8, height: 8, borderRadius: 4 } },

  // 17 occurrences across 16 files
  Box2pV2: { type: 'Pressable', style: { opacity: 0, position: 'absolute' } },

  // 16 occurrences across 13 files
  InlineG8V5: { type: 'Row', style: { justifyContent: 'space-between', alignItems: 'center', gap: 8 } },

  // 15 occurrences across 14 files
  TextPlain7: { type: 'Text', fontSize: 12 },

  // 15 occurrences across 14 files
  BodyPlain4: { type: 'Text', size: 10, style: { letterSpacing: 2 } },

  // 14 occurrences across 12 files
  Box2pV3: { type: 'Box', style: { alignItems: 'center', justifyContent: 'center' } },

  // 14 occurrences across 12 files
  Pad8: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 14 occurrences across 10 files
  Pad8V2: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 13 occurrences across 12 files
  SpacerV2: { type: 'ScrollView', style: { flexGrow: 1 } },

  // 13 occurrences across 11 files
  InlineG41s: { type: 'Row', style: { gap: 4 } },

  // 13 occurrences across 10 files
  InlineV2: { type: 'Row', style: { alignItems: 'center' } },

  // 13 occurrences across 10 files
  Pad10V2: { type: 'Box', style: { padding: 10, borderWidth: 1 } },

  // 13 occurrences across 11 files
  InlineG103s: { type: 'Row', style: { justifyContent: 'space-between', alignItems: 'center', gap: 10 } },

  // 12 occurrences across 5 files
  BoldBody102s: { type: 'Text', size: 10, bold: true },

  // 12 occurrences across 10 files
  Stack3: { type: 'Col', style: { gap: 3 } },

  // 12 occurrences across 12 files
  Body11Plain2: { type: 'Text', size: 11 },

  // 12 occurrences across 12 files
  RoundR1W3: { type: 'Box', style: { width: 3, height: 10, borderRadius: 1 } },

  // 12 occurrences across 9 files
  Pad122s: { type: 'Box', style: { padding: 12, borderWidth: 1 } },

  // 12 occurrences across 9 files
  BodyPlain5: { type: 'Text', size: 10, style: { fontFamily: 'monospace' } },

  // 11 occurrences across 9 files
  BareV2: { type: 'Col', style: { gap: 0 } },

  // 11 occurrences across 8 files
  Box2pW100Pct2: { type: 'Box', style: { width: '100%', height: '100%' } },

  // 11 occurrences across 11 files
  Half: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, flexBasis: 0, minHeight: 0 } },

  // 11 occurrences across 7 files
  Pad10V3: { type: 'Box', style: { padding: 10, borderBottomWidth: 1 } },

  // 11 occurrences across 11 files
  Stack12: { type: 'Col', style: { gap: 12 } },

  // 11 occurrences across 10 files
  SectionG10P12: { type: 'Box', style: { padding: 12, borderWidth: 1, gap: 10 } },

  // 10 occurrences across 6 files
  Stack6V2: { type: 'Box', style: { gap: 6 } },

  // 10 occurrences across 4 files
  Subtitle: { type: 'Text', size: 12, bold: true },

  // 10 occurrences across 7 files
  MedTextPlain: { type: 'Text', size: 12 },

  // 10 occurrences across 9 files
  BoldTextPlain8: { type: 'Text', fontSize: 8, style: { fontWeight: 'bold' } },

  // 10 occurrences across 8 files
  InlineG43s: { type: 'Row', style: { gap: 4, alignItems: 'center', flexWrap: 'wrap' } },

  // 10 occurrences across 9 files
  Dot6: { type: 'Box', style: { width: 6, height: 6, borderRadius: 3 } },

  // 10 occurrences across 8 files
  Pad65s: { type: 'Box', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 3,
      paddingBottom: 3,
      borderWidth: 1,
    } },

  // 10 occurrences across 7 files
  Label3s: { type: 'Text', fontSize: 9, style: { letterSpacing: 0.6, fontWeight: 'bold' } },

  // 10 occurrences across 5 files
  Box3p: { type: 'Box', style: { paddingHorizontal: 8, paddingVertical: 4, borderWidth: 1 } },

  // 10 occurrences across 8 files
  SectionG8P12: { type: 'Box', style: { gap: 8, padding: 12, borderWidth: 1 } },

  // 10 occurrences across 6 files
  Label3s2: { type: 'Text', fontSize: 9, style: { letterSpacing: 0.5, fontWeight: 'bold' } },

  // 10 occurrences across 4 files
  CardR14P14: { type: 'Box', style: {
      padding: 14,
      borderRadius: 14,
      borderWidth: 1,
      gap: 12,
    } },

  // 10 occurrences across 10 files
  BodyPlain6: { type: 'Text', size: 10, style: { flexGrow: 1 } },

  // 10 occurrences across 10 files
  Box3pW16: { type: 'Box', style: { position: 'absolute', width: 16, alignItems: 'center' } },

  // 9 occurrences across 9 files
  HalfV2: { type: 'Box', style: { flexGrow: 1, flexBasis: 0 } },

  // 9 occurrences across 6 files
  BoldBody: { type: 'Text', size: 11, bold: true },

  // 9 occurrences across 5 files
  BareV3: { type: 'Box', style: { position: 'relative' } },

  // 9 occurrences across 7 files
  BoldTextPlain9: { type: 'Text', fontSize: 18, style: { fontWeight: 'bold' } },

  // 9 occurrences across 9 files
  SectionG10P10: { type: 'Col', style: { padding: 10, gap: 10 } },

  // 9 occurrences across 9 files
  CaptionPlain4: { type: 'Text', size: 9, style: { letterSpacing: 2 } },

  // 9 occurrences across 6 files
  HalfGap1: { type: 'Col', style: { gap: 1, flexGrow: 1, flexBasis: 0 } },

  // 9 occurrences across 7 files
  BareV4: { type: 'Box', style: { borderWidth: 1 } },

  // 8 occurrences across 3 files
  Stack2V2: { type: 'Box', style: { gap: 2 } },

  // 8 occurrences across 5 files
  InlineG103s2: { type: 'Row', style: { alignItems: 'center', gap: 10, flexWrap: 'wrap' } },

  // 8 occurrences across 3 files
  CardR8P12: { type: 'Box', style: {
      borderRadius: 8,
      padding: 12,
      gap: 8,
      borderWidth: 1,
    } },

  // 8 occurrences across 8 files
  Stack6V3: { type: 'Col', style: { gap: 6, alignItems: 'center' } },

  // 8 occurrences across 8 files
  HalfV3: { type: 'Row', style: { flexGrow: 1, flexBasis: 0, minHeight: 0 } },

  // 8 occurrences across 8 files
  SectionG8P10: { type: 'Col', style: { gap: 8, padding: 10, borderWidth: 1 } },

  // 8 occurrences across 3 files
  TinyPlain2: { type: 'Text', size: 8 },

  // 8 occurrences across 7 files
  InlineV3: { type: 'Row', style: { flexWrap: 'wrap' } },

  // 8 occurrences across 5 files
  Pad10V4: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 7,
      paddingBottom: 7,
      borderWidth: 1,
    } },

  // 8 occurrences across 6 files
  InlineG122s: { type: 'Row', style: { gap: 12, flexWrap: 'wrap' } },

  // 8 occurrences across 1 files
  Box3pV3: { type: 'Box', style: { paddingHorizontal: 8, paddingVertical: 3, borderWidth: 1 } },

  // 8 occurrences across 5 files
  Pad8V3: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 6,
      paddingBottom: 6,
      borderWidth: 1,
    } },

  // 8 occurrences across 5 files
  SmallBoldPlain: { type: 'Text', size: 9, bold: true },

  // 8 occurrences across 7 files
  TextC657: { type: 'Text', fontSize: 9, color: '#657185' },

  // 8 occurrences across 1 files
  Stack10W100Pct: { type: 'Col', style: { width: '100%', gap: 10 } },

  // 7 occurrences across 5 files
  Box5pW100Pct: { type: 'Graph', viewX: 0, viewY: 0, viewZoom: 1, style: { width: '100%', height: '100%' } },

  // 7 occurrences across 5 files
  BoldTextPlain10: { type: 'Text', fontSize: 9, style: { textTransform: 'uppercase', fontWeight: 'bold' } },

  // 7 occurrences across 5 files
  ChipR4P6: { type: 'Pressable', style: {
      borderRadius: 4,
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
    } },

  // 7 occurrences across 6 files
  TextPlain8: { type: 'Text', fontSize: 10, style: { padding: 8 } },

  // 7 occurrences across 7 files
  Dot10: { type: 'Box', style: { width: 10, height: 10, borderRadius: 5 } },

  // 7 occurrences across 7 files
  HalfV4: { type: 'Col', style: { flexGrow: 1, flexBasis: 0 } },

  // 7 occurrences across 3 files
  ChipR8P8: { type: 'Pressable', style: { padding: 8, borderRadius: 8, borderWidth: 1 } },

  // 7 occurrences across 6 files
  HalfV5: { type: 'Col', style: { flexGrow: 1, flexBasis: 0, minHeight: 0 } },

  // 7 occurrences across 7 files
  Box2pW100Pct3: { type: 'Canvas', style: { width: '100%', height: '100%' } },

  // 7 occurrences across 6 files
  Label3s3: { type: 'Text', fontSize: 10, style: { letterSpacing: 0.8, fontWeight: 'bold' } },

  // 7 occurrences across 4 files
  Pad62s: { type: 'Box', style: { padding: 6, borderWidth: 1 } },

  // 7 occurrences across 5 files
  Pad10V5: { type: 'Box', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 7 occurrences across 7 files
  BoldTextPlain11: { type: 'Text', fontSize: 10, style: { fontFamily: 'monospace', fontWeight: 'bold' } },

  // 7 occurrences across 7 files
  SectionG8P102: { type: 'Box', style: { gap: 8, padding: 10, borderWidth: 1 } },

  // 7 occurrences across 6 files
  SectionG6P10: { type: 'Box', style: { gap: 6, padding: 10, borderWidth: 1 } },

  // 7 occurrences across 7 files
  Label3s4: { type: 'Text', fontSize: 10, style: { letterSpacing: 0.6, fontWeight: 'bold' } },

  // 7 occurrences across 3 files
  DimText1s: { type: 'Text', color: 'theme:textDim' },

  // 7 occurrences across 6 files
  InlineG123s: { type: 'Row', style: { gap: 12, alignItems: 'stretch', flexWrap: 'wrap' } },

  // 7 occurrences across 4 files
  InlineV4: { type: 'Row', style: { alignItems: 'center', flexWrap: 'wrap' } },

  // 7 occurrences across 6 files
  Box2pV6: { type: 'Box', style: { position: 'relative', overflow: 'visible' } },

  // 7 occurrences across 3 files
  Pad162s: { type: 'Box', style: { padding: 16, borderWidth: 1 } },

  // 7 occurrences across 5 files
  CaptionPlain5: { type: 'Text', size: 9, style: { fontFamily: 'monospace' } },

  // 6 occurrences across 2 files
  TextC8b9: { type: 'Text', fontSize: 10, color: '#8b98a6' },

  // 6 occurrences across 4 files
  InlineG122s2: { type: 'Row', style: { gap: 12, alignItems: 'center' } },

  // 6 occurrences across 5 files
  HalfV6: { type: 'ScrollView', style: { flexGrow: 1, flexBasis: 0 } },

  // 6 occurrences across 6 files
  HalfV7: { type: 'Box', style: { flexGrow: 1, flexBasis: 0, minHeight: 0 } },

  // 6 occurrences across 6 files
  HalfGap24s: { type: 'Col', style: {
      gap: 2,
      minWidth: 0,
      flexGrow: 1,
      flexBasis: 0,
    } },

  // 6 occurrences across 5 files
  Pad8V4: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 6 occurrences across 6 files
  Stack1: { type: 'Col', style: { gap: 1 } },

  // 6 occurrences across 2 files
  ChipR4P62: { type: 'Pressable', style: {
      borderRadius: 4,
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 6 occurrences across 4 files
  Pad65s2: { type: 'Box', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 6 occurrences across 6 files
  Body11Plain3: { type: 'Text', size: 11, style: { letterSpacing: 2 } },

  // 6 occurrences across 5 files
  CaptionPlain6: { type: 'Text', size: 9, style: { letterSpacing: 1 } },

  // 6 occurrences across 6 files
  Pad10V6: { type: 'Box', style: { padding: 10 } },

  // 6 occurrences across 4 files
  Pad8V5: { type: 'Pressable', style: { padding: 8, borderWidth: 1 } },

  // 6 occurrences across 6 files
  HalfV8: { type: 'ScrollView', style: { flexGrow: 1, flexBasis: 0, minHeight: 0 } },

  // 6 occurrences across 6 files
  Pad10V7: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 6 occurrences across 6 files
  Pad8V6: { type: 'Box', style: { padding: 8, borderWidth: 1 } },

  // 6 occurrences across 6 files
  Box3pW100Pct: { type: 'Col', style: { width: '100%', height: '100%', minHeight: 0 } },

  // 6 occurrences across 5 files
  SectionG4P10: { type: 'Box', style: { padding: 10, borderWidth: 1, gap: 4 } },

  // 6 occurrences across 5 files
  SpacerV3: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1 } },

  // 6 occurrences across 5 files
  Pad8V7: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 3,
      paddingBottom: 3,
      borderWidth: 1,
    } },

  // 6 occurrences across 6 files
  BoldTextPlain12: { type: 'Text', fontSize: 9, style: { fontFamily: 'monospace', fontWeight: 'bold' } },

  // 6 occurrences across 6 files
  InlineG21s: { type: 'Row', style: { gap: 2 } },

  // 6 occurrences across 6 files
  InlineG102s: { type: 'Row', style: { alignItems: 'center', gap: 10 } },

  // 6 occurrences across 4 files
  BoldTextC000: { type: 'Text', color: '#000', style: { fontWeight: 'bold' } },

  // 6 occurrences across 4 files
  Pad10V8: { type: 'Box', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 7,
      paddingBottom: 7,
      borderWidth: 1,
    } },

  // 6 occurrences across 4 files
  CaptionPlain7: { type: 'Text', size: 9, style: { letterSpacing: 1, textTransform: 'uppercase' } },

  // 5 occurrences across 1 files
  TextC647: { type: 'Text', fontSize: 8, color: '#64748b' },

  // 5 occurrences across 1 files
  BodyC475: { type: 'Text', size: 10, color: '#475569', style: { lineHeight: 10 } },

  // 5 occurrences across 5 files
  TextPlain9: { type: 'Text', fontSize: 10, style: { flexGrow: 1, flexBasis: 0 } },

  // 5 occurrences across 5 files
  InlineG102s2: { type: 'Row', style: { gap: 10, flexWrap: 'wrap' } },

  // 5 occurrences across 2 files
  BadgeR999: { type: 'Box', style: {
      backgroundColor: '#1e1720',
      borderColor: '#f7dfb82b',
      borderWidth: 1,
      borderRadius: 999,
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
    } },

  // 5 occurrences across 2 files
  TextCf5e: { type: 'Text', fontSize: 12, color: '#f5e4bf' },

  // 5 occurrences across 5 files
  Pad44s: { type: 'Box', style: {
      paddingLeft: 4,
      paddingRight: 4,
      paddingTop: 1,
      paddingBottom: 1,
    } },

  // 5 occurrences across 5 files
  BoldTextPlain13: { type: 'Text', fontSize: 10, numberOfLines: 1, style: { fontWeight: 'bold', flexGrow: 1, flexBasis: 0 } },

  // 5 occurrences across 4 files
  InlineV5: { type: 'Row', style: { borderBottomWidth: 1, alignItems: 'center' } },

  // 5 occurrences across 1 files
  CardR8P122: { type: 'Box', style: {
      borderRadius: 8,
      padding: 12,
      gap: 10,
      borderWidth: 1,
    } },

  // 5 occurrences across 5 files
  MedTextPlain2: { type: 'Text', size: 12 },

  // 5 occurrences across 5 files
  RoundR1W4: { type: 'Box', style: { width: 4, height: 12, borderRadius: 1 } },

  // 5 occurrences across 3 files
  Box2pW10: { type: 'Box', style: { width: 10, height: 10 } },

  // 5 occurrences across 4 files
  TextPlain10: { type: 'Text', style: { fontFamily: 'monospace' } },

  // 5 occurrences across 2 files
  Pad55s: { type: 'Box', style: {
      paddingLeft: 5,
      paddingRight: 5,
      paddingTop: 1,
      paddingBottom: 1,
      borderWidth: 1,
    } },

  // 5 occurrences across 4 files
  SectionG6P12: { type: 'Box', style: { padding: 12, gap: 6, borderWidth: 1 } },

  // 5 occurrences across 5 files
  BareV5: { type: 'Box', style: { position: 'absolute' } },

  // 5 occurrences across 4 files
  InlineG8V6: { type: 'Row', style: {
      gap: 8,
      alignItems: 'center',
      justifyContent: 'space-between',
      flexWrap: 'wrap',
    } },

  // 5 occurrences across 3 files
  Stack4V2: { type: 'Box', style: { gap: 4 } },

  // 5 occurrences across 4 files
  BoldTextPlain14: { type: 'Text', fontSize: 16, style: { fontWeight: 'bold' } },

  // 5 occurrences across 4 files
  TextPlain11: { type: 'Text', fontSize: 9, style: { whiteSpace: 'pre' } },

  // 5 occurrences across 5 files
  TextPlain12: { type: 'Text', fontSize: 10, style: { flexShrink: 1, flexBasis: 0 } },

  // 5 occurrences across 4 files
  BoldTextPlain15: { type: 'Text', fontSize: 11, style: { fontFamily: 'monospace', fontWeight: 'bold' } },

  // 5 occurrences across 2 files
  TextPlain13: { type: 'Text', fontSize: 11, style: { fontFamily: 'monospace' } },

  // 5 occurrences across 5 files
  InlineV6: { type: 'Row', style: { alignItems: 'flex-start' } },

  // 5 occurrences across 3 files
  ChipR8P82: { type: 'Pressable', style: { padding: 8, borderRadius: 8 } },

  // 5 occurrences across 3 files
  CardR8P8: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      padding: 8,
      borderRadius: 8,
    } },

  // 5 occurrences across 4 files
  Pad8V8: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, padding: 8 } },

  // 5 occurrences across 2 files
  Box3pV5: { type: 'Pressable', style: { paddingHorizontal: 8, paddingVertical: 3, borderWidth: 1 } },

  // 5 occurrences across 1 files
  SectionG10P14: { type: 'Box', style: { padding: 14, borderWidth: 1, gap: 10 } },

  // 5 occurrences across 2 files
  Pad41s: { type: 'Box', style: { padding: 4 } },

  // 5 occurrences across 5 files
  Label3s5: { type: 'Text', fontSize: 10, style: { letterSpacing: 0.7, fontWeight: 'bold' } },

  // 5 occurrences across 3 files
  MicroNormal: { type: 'Text', size: 7, style: { fontFamily: 'monospace' } },

  // 5 occurrences across 3 files
  TinyBold3s: { type: 'Text', size: 8, bold: true, style: { fontFamily: 'monospace' } },

  // 5 occurrences across 5 files
  InlineV7: { type: 'Row', style: { borderWidth: 1 } },

  // 4 occurrences across 4 files
  TextCfff: { type: 'Text', fontSize: 14, color: '#ffffff' },

  // 4 occurrences across 2 files
  TextC94a: { type: 'Text', fontSize: 10, color: '#94a3b8' },

  // 4 occurrences across 3 files
  ChipR4P8: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 4,
      paddingBottom: 4,
      borderRadius: 4,
    } },

  // 4 occurrences across 2 files
  BoldTextC8b9: { type: 'Text', fontSize: 10, color: '#8b98a6', style: { fontWeight: 'bold' } },

  // 4 occurrences across 1 files
  MedTextCf1f: { type: 'Text', size: 13, color: '#f1f5f9' },

  // 4 occurrences across 4 files
  SpacerG2: { type: 'Col', style: { flexGrow: 1, gap: 2 } },

  // 4 occurrences across 2 files
  BodyC4752: { type: 'Text', size: 10, color: '#475569' },

  // 4 occurrences across 3 files
  TextPlain14: { type: 'Text', fontSize: 13 },

  // 4 occurrences across 4 files
  SpacerV5: { type: 'Row', style: { flexGrow: 1 } },

  // 4 occurrences across 4 files
  InlineG102s3: { type: 'Row', style: { gap: 10, alignItems: 'flex-start' } },

  // 4 occurrences across 4 files
  Box2pW100Pct4: { type: 'Pressable', style: { width: '100%', height: '100%' } },

  // 4 occurrences across 1 files
  InlineG62s3: { type: 'Box', style: { flexDirection: 'row', gap: 6 } },

  // 4 occurrences across 1 files
  TextC222: { type: 'Text', fontSize: 8, color: '#222222' },

  // 4 occurrences across 2 files
  ChipR4P82: { type: 'Pressable', style: {
      borderRadius: 4,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 3,
      paddingBottom: 3,
      borderWidth: 1,
    } },

  // 4 occurrences across 2 files
  RoundR6H80: { type: 'Box', style: { height: 80, borderRadius: 6, borderWidth: 1 } },

  // 4 occurrences across 4 files
  ChipR4P63: { type: 'Box', style: {
      borderRadius: 4,
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 4 occurrences across 3 files
  Box3pW100Pct2: { type: 'Box', style: { width: '100%', height: '100%', overflow: 'hidden' } },

  // 4 occurrences across 4 files
  Body11Plain4: { type: 'Text', size: 11, style: { letterSpacing: 1 } },

  // 4 occurrences across 4 files
  InlineG8V7: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      paddingHorizontal: 10,
      paddingVertical: 6,
      borderBottomWidth: 1,
    } },

  // 4 occurrences across 4 files
  InlineG32s: { type: 'Row', style: { alignItems: 'center', gap: 3 } },

  // 4 occurrences across 1 files
  CardR10P8: { type: 'Pressable', style: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 8,
      padding: 8,
      borderRadius: 10,
    } },

  // 4 occurrences across 4 files
  Box2pW100Pct5: { type: 'ScrollView', style: { width: '100%', height: '100%' } },

  // 4 occurrences across 4 files
  BandG108s: { type: 'Row', style: {
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10,
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 10,
      paddingBottom: 10,
      borderBottomWidth: 1,
    } },

  // 4 occurrences across 2 files
  ChipR8P6: { type: 'Pressable', style: { padding: 6, borderRadius: 8, borderWidth: 1 } },

  // 4 occurrences across 4 files
  SectionG8P103: { type: 'Pressable', style: { padding: 10, borderWidth: 1, gap: 8 } },

  // 4 occurrences across 2 files
  Box3pV7: { type: 'Pressable', style: { paddingHorizontal: 8, paddingVertical: 5, borderWidth: 1 } },

  // 4 occurrences across 4 files
  SectionG6P102: { type: 'Col', style: { gap: 6, padding: 10, borderWidth: 1 } },

  // 4 occurrences across 4 files
  Box2pV10: { type: 'Box', style: { height: 4, overflow: 'hidden' } },

  // 4 occurrences across 4 files
  BareV6: { type: 'Box', style: { height: 4 } },

  // 4 occurrences across 4 files
  TextPlain15: { type: 'Text', fontSize: 9, numberOfLines: 1 },

  // 4 occurrences across 2 files
  BandG6P8: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
      flexDirection: 'row',
      gap: 6,
      alignItems: 'center',
    } },

  // 4 occurrences across 4 files
  BandG12: { type: 'Row', style: {
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      paddingLeft: 14,
      paddingRight: 14,
      paddingTop: 12,
      paddingBottom: 12,
      borderBottomWidth: 1,
    } },

  // 4 occurrences across 4 files
  SectionG12P14: { type: 'Col', style: { padding: 14, gap: 12 } },

  // 4 occurrences across 4 files
  Label3s6: { type: 'Text', fontSize: 10, style: { letterSpacing: 1.1, fontWeight: 'bold' } },

  // 4 occurrences across 2 files
  CardR10P10: { type: 'Col', style: { gap: 8, padding: 10, borderRadius: 10 } },

  // 4 occurrences across 3 files
  TextPlain16: { type: 'Text', fontSize: 8, style: { fontFamily: 'monospace' } },

  // 4 occurrences across 4 files
  RoundR4: { type: 'Box', style: {
      flexGrow: 1,
      borderRadius: 4,
      borderWidth: 1,
      paddingHorizontal: 8,
      paddingVertical: 4,
    } },

  // 4 occurrences across 4 files
  CardR6G8: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      padding: 8,
      borderRadius: 6,
      borderWidth: 1,
    } },

  // 4 occurrences across 4 files
  RoundR8: { type: 'Col', style: { borderWidth: 1, borderRadius: 8, overflow: 'hidden' } },

  // 4 occurrences across 2 files
  ChipR6P8: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 4,
      paddingBottom: 4,
      borderRadius: 6,
      borderWidth: 1,
    } },

  // 4 occurrences across 4 files
  InlineG122s3: { type: 'Row', style: { gap: 12, alignItems: 'flex-start' } },

  // 4 occurrences across 2 files
  CaptionC5c6: { type: 'Text', size: 9, color: '#5c6a78' },

  // 4 occurrences across 3 files
  BodyC5c6: { type: 'Text', size: 10, color: '#5c6a78' },

  // 4 occurrences across 2 files
  HalfGap6: { type: 'Col', style: {
      gap: 6,
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 220,
      overflow: 'visible',
    } },

  // 4 occurrences across 4 files
  BareV7: { type: 'ScrollView', showScrollbar: true },

  // 4 occurrences across 4 files
  InlineG103s3: { type: 'Row', style: { justifyContent: 'space-between', gap: 10, flexWrap: 'wrap' } },

  // 4 occurrences across 3 files
  Pad165s: { type: 'Pressable', style: {
      paddingLeft: 16,
      paddingRight: 16,
      paddingTop: 9,
      paddingBottom: 9,
      borderWidth: 1,
    } },

  // 4 occurrences across 4 files
  Box4pW100Pct: { type: 'Box', style: {
      width: '100%',
      height: '100%',
      justifyContent: 'center',
      alignItems: 'center',
    } },

  // 4 occurrences across 1 files
  InlineG8W100Pct: { type: 'Row', style: {
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 8,
    } },

  // 4 occurrences across 4 files
  Box3pW40: { type: 'Box', style: { position: 'absolute', width: 40, alignItems: 'center' } },

  // 4 occurrences across 4 files
  Box4pW24: { type: 'Pressable', style: {
      opacity: 0,
      position: 'absolute',
      width: 24,
      height: 24,
    } },

  // 4 occurrences across 4 files
  TinyPlain3: { type: 'Text', size: 8, style: { letterSpacing: 1, textTransform: 'uppercase' } },

  // 4 occurrences across 2 files
  CaptionPlain8: { type: 'Text', size: 9, style: { letterSpacing: 1.1, textTransform: 'uppercase' } },

  // 4 occurrences across 4 files
  MedTextPlain3: { type: 'Text', size: 13 },

  // 4 occurrences across 2 files
  InlineG8W100Pct2: { type: 'Row', style: { width: '100%', gap: 8, justifyContent: 'center' } },

  // 4 occurrences across 4 files
  Stack40: { type: 'Col', style: { width: '100%', gap: 40 } },

  // 4 occurrences across 1 files
  Stack6W100Pct: { type: 'Col', style: { width: '100%', gap: 6, alignItems: 'flex-start' } },

  // 4 occurrences across 1 files
  SmallBoldC7f9: { type: 'Text', size: 9, bold: true, color: '#7f97ab' },

  // 3 occurrences across 3 files
  CardR8P14: { type: 'Pressable', style: {
      alignSelf: 'flex-start',
      paddingTop: 10,
      paddingBottom: 10,
      paddingLeft: 14,
      paddingRight: 14,
      borderRadius: 8,
      backgroundColor: '#1d4ed8',
    } },

  // 3 occurrences across 3 files
  TextCf8f: { type: 'Text', fontSize: 18, color: '#f8fafc' },

  // 3 occurrences across 1 files
  TextCfff2: { type: 'Text', fontSize: 7, color: '#ffffff' },

  // 3 occurrences across 1 files
  InlineG42s3: { type: 'Box', style: { flexDirection: 'row', gap: 4 } },

  // 3 occurrences across 1 files
  TextC475: { type: 'Text', fontSize: 7, color: '#475569' },

  // 3 occurrences across 1 files
  TextC6472: { type: 'Text', fontSize: 7, color: '#64748b' },

  // 3 occurrences across 1 files
  TextCe2e: { type: 'Text', fontSize: 12, color: '#e2e8f0' },

  // 3 occurrences across 3 files
  InlineG123s2: { type: 'Row', style: { justifyContent: 'space-between', alignItems: 'center', gap: 12 } },

  // 3 occurrences across 3 files
  SpacerV6: { type: 'Box', style: { flexGrow: 1, justifyContent: 'center', alignItems: 'center' } },

  // 3 occurrences across 3 files
  Box4pW100Pct2: { type: 'Col', style: {
      width: '100%',
      height: '100%',
      minWidth: 0,
      minHeight: 0,
    } },

  // 3 occurrences across 1 files
  TextC93c: { type: 'Text', fontSize: 10, color: '#93c5fd' },

  // 3 occurrences across 3 files
  TextPlain17: { type: 'Text', fontSize: 10, style: { lineHeight: 14 } },

  // 3 occurrences across 3 files
  BareV8: { type: 'Box', style: { flexShrink: 0 } },

  // 3 occurrences across 3 files
  SubtitleV2: { type: 'Text', size: 13, bold: true },

  // 3 occurrences across 3 files
  SectionHead: { type: 'Text', size: 14, bold: true },

  // 3 occurrences across 2 files
  InlineV8: { type: 'Row', style: { justifyContent: 'space-between' } },

  // 3 occurrences across 3 files
  Box3pW100Pct3: { type: 'Box', style: { width: '100%', height: '100%', backgroundColor: '#000000' } },

  // 3 occurrences across 1 files
  InlineV9: { type: 'Box', style: { flexDirection: 'row', justifyContent: 'spaceBetween', alignItems: 'center' } },

  // 3 occurrences across 2 files
  TextPlain18: { type: 'Text', fontSize: 7 },

  // 3 occurrences across 1 files
  TextC2222: { type: 'Text', fontSize: 9, color: '#222222' },

  // 3 occurrences across 1 files
  TextC6473: { type: 'Text', fontSize: 10, color: '#64748b' },

  // 3 occurrences across 1 files
  MedTextC94a: { type: 'Text', size: 12, color: '#94a3b8' },

  // 3 occurrences across 1 files
  LargeBodyCcbd: { type: 'Text', size: 14, color: '#cbd5e1' },

  // 3 occurrences across 3 files
  SpacerV7: { type: 'Col', style: { flexGrow: 1, gap: 0 } },

  // 3 occurrences across 3 files
  BandG6P82: { type: 'Row', style: {
      padding: 8,
      gap: 6,
      alignItems: 'center',
      borderBottomWidth: 1,
    } },

  // 3 occurrences across 3 files
  SectionG12P12: { type: 'Col', style: { flexGrow: 1, padding: 12, gap: 12 } },

  // 3 occurrences across 1 files
  CardR8P123: { type: 'Box', style: {
      borderRadius: 8,
      padding: 12,
      gap: 4,
      minWidth: 120,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  RoundR4W16: { type: 'Box', style: {
      width: 16,
      height: 16,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  ChipR4P83: { type: 'Pressable', style: {
      borderRadius: 4,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  SectionG8P104: { type: 'Col', style: { gap: 8, padding: 10 } },

  // 3 occurrences across 1 files
  CardR6G4: { type: 'Pressable', style: {
      borderRadius: 6,
      padding: 8,
      gap: 4,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  RoundR4V3: { type: 'Pressable', style: {
      paddingHorizontal: 8,
      paddingVertical: 4,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  BareV9: { type: 'ScrollView', style: { maxHeight: 280 } },

  // 3 occurrences across 1 files
  Pad8V9: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 3,
      paddingBottom: 3,
    } },

  // 3 occurrences across 3 files
  BareV10: { type: 'TextInput', style: { fontSize: 11 } },

  // 3 occurrences across 3 files
  Pad8V10: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  HalfGap24s2: { type: 'Col', style: {
      gap: 2,
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 220,
    } },

  // 3 occurrences across 3 files
  SectionG10P122: { type: 'Col', style: { gap: 10, padding: 12, borderBottomWidth: 1 } },

  // 3 occurrences across 2 files
  Pad121s: { type: 'Box', style: { padding: 12 } },

  // 3 occurrences across 3 files
  HalfGap12: { type: 'Row', style: {
      flexGrow: 1,
      flexBasis: 0,
      minHeight: 0,
      gap: 12,
      padding: 12,
    } },

  // 3 occurrences across 3 files
  SectionG6P8: { type: 'Col', style: { padding: 8, gap: 6 } },

  // 3 occurrences across 3 files
  Box3pV10: { type: 'Pressable', style: { paddingHorizontal: 10, paddingVertical: 6, borderWidth: 1 } },

  // 3 occurrences across 1 files
  Box3pV11: { type: 'Pressable', style: { paddingHorizontal: 10, paddingVertical: 7, borderWidth: 1 } },

  // 3 occurrences across 2 files
  Box5pW44: { type: 'Box', style: {
      width: 44,
      height: '100%',
      justifyContent: 'center',
      alignItems: 'flex-end',
      paddingRight: 6,
    } },

  // 3 occurrences across 2 files
  HalfP4: { type: 'Box', style: {
      flexGrow: 1,
      flexShrink: 1,
      flexBasis: 0,
      minWidth: 0,
      height: '100%',
      justifyContent: 'center',
      paddingLeft: 4,
      overflow: 'hidden',
    } },

  // 3 occurrences across 3 files
  HalfV10: { type: 'Col', style: { flexGrow: 1, flexShrink: 1, flexBasis: 0 } },

  // 3 occurrences across 3 files
  InlineP12: { type: 'Row', style: {
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: 12,
      borderBottomWidth: 1,
    } },

  // 3 occurrences across 3 files
  BareV11: { type: 'Pressable', style: { borderWidth: 1 } },

  // 3 occurrences across 3 files
  InlineG103s4: { type: 'Row', style: { gap: 10, flexWrap: 'wrap', alignItems: 'flex-start' } },

  // 3 occurrences across 2 files
  Pad65s3: { type: 'Pressable', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  InlineG8V10: { type: 'Row', style: { gap: 8, flexWrap: 'wrap', alignItems: 'flex-start' } },

  // 3 occurrences across 2 files
  Divider3s: { type: 'Box', style: { height: 1, marginTop: 4, marginBottom: 4 } },

  // 3 occurrences across 1 files
  Box3pW1: { type: 'Box', style: { width: 1, height: 14, marginHorizontal: 4 } },

  // 3 occurrences across 3 files
  Pad10H32: { type: 'TextInput', style: {
      height: 32,
      borderWidth: 1,
      paddingLeft: 10,
      paddingRight: 10,
    } },

  // 3 occurrences across 1 files
  TextPlain19: { type: 'Text', fontSize: 9, style: { marginBottom: 4 } },

  // 3 occurrences across 2 files
  ChipR6P6: { type: 'Pressable', style: { padding: 6, borderRadius: 6, borderWidth: 1 } },

  // 3 occurrences across 3 files
  SpacerV8: { type: 'Box', style: { flexGrow: 1, overflow: 'hidden' } },

  // 3 occurrences across 2 files
  TinyBold2s: { type: 'Text', size: 8, bold: true },

  // 3 occurrences across 2 files
  Stack4V3: { type: 'Col', style: { gap: 4, alignItems: 'center' } },

  // 3 occurrences across 3 files
  Stack3V2: { type: 'Col', style: { gap: 3, alignItems: 'center' } },

  // 3 occurrences across 3 files
  CardR4: { type: 'Col', style: {
      gap: 2,
      padding: 6,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  CardR8P82: { type: 'Col', style: {
      gap: 6,
      padding: 8,
      borderWidth: 1,
      borderRadius: 8,
    } },

  // 3 occurrences across 3 files
  HalfGap43s: { type: 'Col', style: { gap: 4, flexGrow: 1, flexBasis: 0 } },

  // 3 occurrences across 3 files
  ChipR999: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 4,
      paddingBottom: 4,
      borderRadius: 999,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  BadgeR12P12: { type: 'Box', style: { padding: 12, borderRadius: 12, borderWidth: 1 } },

  // 3 occurrences across 3 files
  SpacerV9: { type: 'Col', style: { flexGrow: 1, borderTopWidth: 1 } },

  // 3 occurrences across 3 files
  TextPlain20: { type: 'Text', fontSize: 9, style: { whiteSpace: 'pre-wrap' } },

  // 3 occurrences across 3 files
  BandG8P10: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      padding: 10,
      borderBottomWidth: 1,
    } },

  // 3 occurrences across 2 files
  HalfV11: { type: 'Box', style: { flexGrow: 1, flexShrink: 1, flexBasis: 0 } },

  // 3 occurrences across 3 files
  Box2pV11: { type: 'ScrollView', style: { maxHeight: 180, borderWidth: 1 } },

  // 3 occurrences across 3 files
  InlineV11: { type: 'Row', style: { alignItems: 'center', borderWidth: 1 } },

  // 3 occurrences across 1 files
  SpacerG4: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 260,
      minHeight: 220,
      gap: 4,
    } },

  // 3 occurrences across 3 files
  Box5pV3: { type: 'Pressable', style: {
      position: 'absolute',
      left: 0,
      top: 0,
      right: 0,
      bottom: 0,
    } },

  // 3 occurrences across 3 files
  BandG6P83: { type: 'Pressable', style: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 6,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 3 occurrences across 3 files
  Box2pV12: { type: 'ScrollView', showScrollbar: true, style: { maxHeight: 180 } },

  // 3 occurrences across 3 files
  Box3pW100Pct4: { type: 'Box', style: { width: '100%', height: '100%', position: 'relative' } },

  // 3 occurrences across 3 files
  Box4pV4: { type: 'Box', style: {
      position: 'absolute',
      left: 0,
      top: 0,
      bottom: 0,
    } },

  // 3 occurrences across 2 files
  Box5pW28: { type: 'Pressable', style: {
      width: 28,
      height: 28,
      borderWidth: 1,
      justifyContent: 'center',
      alignItems: 'center',
    } },

  // 3 occurrences across 2 files
  Stack6V5: { type: 'Col', style: { gap: 6, position: 'relative', overflow: 'visible' } },

  // 3 occurrences across 3 files
  BandG6P4: { type: 'Row', style: { alignItems: 'center', gap: 6, paddingLeft: 4 } },

  // 3 occurrences across 3 files
  SpacerV11: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, height: '100%' } },

  // 3 occurrences across 3 files
  HalfV12: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 0,
      minHeight: 0,
    } },

  // 3 occurrences across 3 files
  HalfGap3: { type: 'Col', style: { flexGrow: 1, flexBasis: 0, gap: 3 } },

  // 3 occurrences across 3 files
  ChipR4P64: { type: 'Box', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
      borderRadius: 4,
    } },

  // 3 occurrences across 3 files
  InlineG63s2: { type: 'Row', style: { gap: 6, alignItems: 'center', justifyContent: 'space-between' } },

  // 3 occurrences across 2 files
  InlineG8V11: { type: 'Row', style: { gap: 8, alignItems: 'stretch' } },

  // 3 occurrences across 1 files
  ChipR8P83: { type: 'TextInput', style: {
      height: 32,
      borderWidth: 1,
      borderRadius: 8,
      paddingLeft: 8,
    } },

  // 3 occurrences across 3 files
  InlineG104s: { type: 'Row', style: {
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10,
      flexWrap: 'wrap',
    } },

  // 3 occurrences across 3 files
  BadgeR12P10: { type: 'Box', style: { padding: 10, borderRadius: 12, borderWidth: 1 } },

  // 3 occurrences across 3 files
  InlineG8V12: { type: 'Row', style: {
      gap: 8,
      alignItems: 'center',
      flexWrap: 'wrap',
      justifyContent: 'flex-end',
    } },

  // 3 occurrences across 2 files
  Stack4W100Pct: { type: 'Col', style: { width: '100%', gap: 4 } },

  // 3 occurrences across 2 files
  Stack8W100Pct: { type: 'Col', style: { width: '100%', gap: 8 } },

  // 3 occurrences across 2 files
  Stack3W100Pct: { type: 'Col', style: { width: '100%', gap: 3 } },

  // 3 occurrences across 2 files
  MicroBold: { type: 'Text', size: 7, bold: true, style: { fontFamily: 'monospace' } },

  // 3 occurrences across 3 files
  InlineG7: { type: 'Row', style: { alignItems: 'center', gap: 7 } },

  // 3 occurrences across 1 files
  Stack5: { type: 'Col', style: { gap: 5 } },

  // 3 occurrences across 3 files
  Box2pV13: { type: 'Pressable', style: { height: 14, position: 'relative' } },

  // 3 occurrences across 2 files
  Box5pW3: { type: 'Box', style: {
      position: 'absolute',
      top: 1,
      marginLeft: -1.5,
      width: 3,
      height: 12,
    } },

  // 3 occurrences across 3 files
  Box2pW4: { type: 'Box', style: { width: 4, height: 4 } },

  // 3 occurrences across 1 files
  InlineG18: { type: 'Row', style: {
      width: '100%',
      gap: 18,
      justifyContent: 'center',
      alignItems: 'flex-end',
    } },

  // 3 occurrences across 3 files
  BoldBody103s: { type: 'Text', size: 10, bold: true, style: { fontFamily: 'monospace' } },

  // 2 occurrences across 2 files
  SectionG12P20: { type: 'Box', style: {
      width: '100%',
      height: '100%',
      padding: 20,
      gap: 12,
      backgroundColor: '#0b1020',
    } },

  // 2 occurrences across 2 files
  TextC94a2: { type: 'Text', fontSize: 12, color: '#94a3b8' },

  // 2 occurrences across 2 files
  HalfV13: { type: 'Box', style: {
      flexGrow: 1,
      flexBasis: 0,
      minHeight: 0,
      borderWidth: 1,
      borderColor: '#1e293b',
      borderRadius: 6,
    } },

  // 2 occurrences across 1 files
  TextC4752: { type: 'Text', fontSize: 6, color: '#475569' },

  // 2 occurrences across 2 files
  InlineG32s2: { type: 'Box', style: { flexDirection: 'row', gap: 3 } },

  // 2 occurrences across 1 files
  ChipR3: { type: 'Pressable', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 3,
      paddingBottom: 3,
      borderRadius: 3,
    } },

  // 2 occurrences across 1 files
  InlineG43s2: { type: 'Box', style: { flexDirection: 'row', gap: 4, alignItems: 'center' } },

  // 2 occurrences across 1 files
  TextCfff3: { type: 'Text', fontSize: 6, color: '#ffffff' },

  // 2 occurrences across 2 files
  SpacerG42: { type: 'ScrollView', style: { flexGrow: 1, gap: 4 } },

  // 2 occurrences across 2 files
  BadgeR8P10: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 7,
      paddingBottom: 7,
      borderRadius: 8,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  InlineG8V13: { type: 'Row', style: { gap: 8, flexWrap: 'wrap', justifyContent: 'flex-end' } },

  // 2 occurrences across 1 files
  BadgeR6: { type: 'Pressable', style: {
      paddingTop: 6,
      paddingBottom: 6,
      paddingLeft: 12,
      paddingRight: 12,
      borderRadius: 6,
    } },

  // 2 occurrences across 1 files
  BadgeR46s: { type: 'Pressable', style: {
      paddingTop: 4,
      paddingBottom: 4,
      paddingLeft: 10,
      paddingRight: 10,
      borderRadius: 4,
      backgroundColor: '#1e293b',
    } },

  // 2 occurrences across 1 files
  TextCf8f2: { type: 'Text', fontSize: 11, color: '#f8fafc' },

  // 2 occurrences across 1 files
  TextCfbb: { type: 'Text', fontSize: 10, color: '#fbbf24' },

  // 2 occurrences across 1 files
  TextC67e: { type: 'Text', fontSize: 10, color: '#67e8f9' },

  // 2 occurrences across 1 files
  TextC7dd: { type: 'Text', fontSize: 10, color: '#7dd3fc' },

  // 2 occurrences across 2 files
  LargeTextCcbd: { type: 'Text', size: 16, color: '#cbd5e1', style: { margin: 0 } },

  // 2 occurrences across 2 files
  HalfV14: { type: 'Canvas', style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  Box3pW100Pct5: { type: 'Box', style: { flexDirection: 'column', width: '100%', height: '100%' } },

  // 2 occurrences across 2 files
  DimText2s: { type: 'Text', fontSize: 9, color: 'theme:textDim' },

  // 2 occurrences across 1 files
  DimText2s2: { type: 'Text', fontSize: 10, color: 'theme:textDim' },

  // 2 occurrences across 1 files
  TextC34D: { type: 'Text', fontSize: 10, color: '#34D399' },

  // 2 occurrences across 1 files
  TextC6B7: { type: 'Text', fontSize: 10, color: '#6B7585' },

  // 2 occurrences across 1 files
  TextC6474: { type: 'Text', fontSize: 10, color: '#64748B' },

  // 2 occurrences across 1 files
  HalfV15: { type: 'Box', style: { flexGrow: 1, flexBasis: 0, paddingBottom: 8 } },

  // 2 occurrences across 1 files
  HalfV16: { type: 'Box', style: { flexGrow: 1, flexBasis: 0, paddingBottom: 10 } },

  // 2 occurrences across 2 files
  Pad41s2: { type: 'Box', style: { paddingTop: 4 } },

  // 2 occurrences across 1 files
  BodyC737: { type: 'Text', size: 10, color: '#737373' },

  // 2 occurrences across 2 files
  Pad124s: { type: 'Pressable', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 6,
      paddingBottom: 6,
    } },

  // 2 occurrences across 2 files
  HalfGap8: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 0,
      gap: 8,
    } },

  // 2 occurrences across 2 files
  Title: { type: 'Text', size: 24, bold: true },

  // 2 occurrences across 2 files
  InlineG121s: { type: 'Row', style: { gap: 12 } },

  // 2 occurrences across 2 files
  InlineG16: { type: 'Row', style: { gap: 16, alignItems: 'stretch' } },

  // 2 occurrences across 1 files
  HalfGap10: { type: 'Box', style: { flexGrow: 1, flexBasis: 0, gap: 10 } },

  // 2 occurrences across 2 files
  Heading: { type: 'Text', size: 18, bold: true },

  // 2 occurrences across 2 files
  MedTextPlain4: { type: 'Text', size: 12, style: { lineHeight: 18 } },

  // 2 occurrences across 1 files
  InlineG23s: { type: 'Row', style: { gap: 2, alignItems: 'center', flexShrink: 0 } },

  // 2 occurrences across 1 files
  BoldBody103s2: { type: 'Text', size: 10, bold: true, numberOfLines: 1 },

  // 2 occurrences across 1 files
  BodyPlain7: { type: 'Text', size: 10, numberOfLines: 1 },

  // 2 occurrences across 1 files
  TextCa0a: { type: 'Text', style: { color: '#a0a8b5' } },

  // 2 occurrences across 2 files
  Box3pW100Pct6: { type: 'Box', style: { position: 'absolute', width: '100%', height: '100%' } },

  // 2 occurrences across 2 files
  Box3pW100Pct7: { type: 'Box', style: { width: '100%', height: '100%', backgroundColor: '#0a0d12' } },

  // 2 occurrences across 2 files
  SectionG18: { type: 'Box', style: {
      width: '100%',
      height: '100%',
      padding: 28,
      gap: 18,
    } },

  // 2 occurrences across 2 files
  CardR26: { type: 'Box', style: {
      width: 460,
      backgroundColor: '#110d11d8',
      borderColor: '#f3d5a21f',
      borderWidth: 1,
      borderRadius: 26,
      padding: 22,
      gap: 10,
    } },

  // 2 occurrences across 2 files
  TextCfff4: { type: 'Text', fontSize: 34, color: '#fff1d0' },

  // 2 occurrences across 2 files
  TextCd7c: { type: 'Text', fontSize: 14, color: '#d7c7b2' },

  // 2 occurrences across 2 files
  RoundR32: { type: 'Box', style: {
      flexGrow: 1,
      minHeight: 0,
      backgroundColor: '#0f11161c',
      borderColor: '#ffe8b514',
      borderWidth: 1,
      borderRadius: 32,
      overflow: 'hidden',
    } },

  // 2 occurrences across 2 files
  BoldTextPlain16: { type: 'Text', fontSize: 11, numberOfLines: 1, style: { fontWeight: 'bold', flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  Pad10V10: { type: 'Box', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  InlineG123s3: { type: 'Row', style: { gap: 12, flexWrap: 'wrap', alignItems: 'flex-start' } },

  // 2 occurrences across 1 files
  RoundR10: { type: 'Pressable', style: {
      width: 28,
      height: 20,
      borderRadius: 10,
      backgroundColor: '#2e2e2e',
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 1 files
  TextCf6f: { type: 'Text', fontSize: 10, color: '#f6f1e6' },

  // 2 occurrences across 1 files
  TextC8a8: { type: 'Text', fontSize: 8, color: '#8a8a8a' },

  // 2 occurrences across 1 files
  LargeBodyCfff: { type: 'Text', size: 14, color: '#ffffff' },

  // 2 occurrences across 1 files
  InlineG24: { type: 'Box', style: { flexDirection: 'row', gap: 24 } },

  // 2 occurrences across 1 files
  CardR8P16: { type: 'Box', style: {
      flexDirection: 'column',
      gap: 6,
      padding: 16,
      backgroundColor: '#1e293b',
      borderRadius: 8,
      width: 260,
    } },

  // 2 occurrences across 1 files
  Body11C647: { type: 'Text', size: 11, color: '#64748b' },

  // 2 occurrences across 1 files
  ChipR4P84: { type: 'Box', style: {
      borderRadius: 4,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 3,
      paddingBottom: 3,
    } },

  // 2 occurrences across 2 files
  BadgeR47s: { type: 'Pressable', style: {
      marginLeft: 'auto',
      borderRadius: 4,
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  TextPlain21: { type: 'Text', fontSize: 10, style: { padding: 12 } },

  // 2 occurrences across 2 files
  RoundR2W10: { type: 'Box', style: { width: 10, height: 10, borderRadius: 2 } },

  // 2 occurrences across 2 files
  SpacerG8: { type: 'Col', style: {
      flexGrow: 1,
      alignItems: 'center',
      justifyContent: 'center',
      gap: 8,
    } },

  // 2 occurrences across 2 files
  InlineG63s3: { type: 'Row', style: { gap: 6, alignItems: 'center', marginTop: 4 } },

  // 2 occurrences across 2 files
  CardR6G82: { type: 'Row', style: {
      borderRadius: 6,
      padding: 8,
      gap: 8,
      alignItems: 'center',
      justifyContent: 'space-between',
      borderWidth: 1,
      borderColor: 'transparent',
    } },

  // 2 occurrences across 2 files
  SpacerG82: { type: 'Row', style: {
      gap: 8,
      alignItems: 'center',
      flexGrow: 1,
      overflow: 'hidden',
    } },

  // 2 occurrences across 2 files
  BoldTextPlain17: { type: 'Text', fontSize: 10, style: { minWidth: 100, fontWeight: 'bold' } },

  // 2 occurrences across 2 files
  TextPlain22: { type: 'Text', fontSize: 10, numberOfLines: 1, style: { flexGrow: 1 } },

  // 2 occurrences across 2 files
  ChipR4P65: { type: 'Pressable', style: {
      backgroundColor: '#5a1d1d',
      borderRadius: 4,
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 2,
      paddingBottom: 2,
    } },

  // 2 occurrences across 1 files
  BadgeR6V2: { type: 'Pressable', style: {
      borderRadius: 6,
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 5,
      paddingBottom: 5,
    } },

  // 2 occurrences across 2 files
  ChipR6P82: { type: 'Row', style: {
      borderRadius: 6,
      padding: 8,
      justifyContent: 'space-between',
      alignItems: 'center',
    } },

  // 2 occurrences across 1 files
  TextPlain23: { type: 'Text', fontSize: 8, style: { marginBottom: 4 } },

  // 2 occurrences across 1 files
  Pad8V11: { type: 'TextInput', fontSize: 11, style: { borderWidth: 1, padding: 8 } },

  // 2 occurrences across 1 files
  BoldTextCenter: { type: 'Text', fontSize: 11, style: { fontWeight: 'bold', textAlign: 'center' } },

  // 2 occurrences across 2 files
  BoldTextPlain18: { type: 'Text', fontSize: 13, style: { fontWeight: 'bold', flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  BandG6P42: { type: 'Row', style: { alignItems: 'center', gap: 6, padding: 4 } },

  // 2 occurrences across 2 files
  Body11Plain5: { type: 'Text', size: 11, style: { letterSpacing: 1 } },

  // 2 occurrences across 2 files
  RoundR999: { type: 'Pressable', style: {
      paddingHorizontal: 7,
      paddingVertical: 3,
      borderRadius: 999,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Body11Plain6: { type: 'Text', size: 11, style: { padding: 8 } },

  // 2 occurrences across 2 files
  RoundR4V4: { type: 'Box', style: {
      borderWidth: 1,
      borderRadius: 4,
      paddingHorizontal: 8,
      paddingVertical: 4,
    } },

  // 2 occurrences across 2 files
  RoundR4V5: { type: 'Pressable', style: {
      paddingHorizontal: 7,
      paddingVertical: 3,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  CardR6G6: { type: 'Col', style: {
      borderWidth: 1,
      borderRadius: 6,
      padding: 8,
      gap: 6,
    } },

  // 2 occurrences across 2 files
  RoundR4V6: { type: 'Box', style: {
      borderWidth: 1,
      borderRadius: 4,
      paddingHorizontal: 6,
      paddingVertical: 2,
    } },

  // 2 occurrences across 2 files
  RoundR4V7: { type: 'Pressable', style: {
      paddingHorizontal: 6,
      paddingVertical: 2,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  InlineG5: { type: 'Row', style: { gap: 5, alignItems: 'center' } },

  // 2 occurrences across 1 files
  InlineG5V2: { type: 'Row', style: { gap: 5, flexWrap: 'wrap' } },

  // 2 occurrences across 2 files
  Box2pW8: { type: 'Box', style: { width: 8, height: 8 } },

  // 2 occurrences across 2 files
  Pad7: { type: 'Box', style: {
      paddingLeft: 7,
      paddingRight: 7,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  RoundR999V2: { type: 'Pressable', style: {
      paddingHorizontal: 6,
      paddingVertical: 2,
      borderRadius: 999,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  CaptionFixed: { type: 'Text', size: 9, style: { width: 12 } },

  // 2 occurrences across 1 files
  CaptionFixedRightW32: { type: 'Text', size: 9, style: { width: 32, textAlign: 'right' } },

  // 2 occurrences across 1 files
  BodyFixedRight: { type: 'Text', size: 10, style: { width: 40, textAlign: 'right' } },

  // 2 occurrences across 2 files
  RoundR2V2: { type: 'Box', style: { borderRadius: 2, paddingHorizontal: 1 } },

  // 2 occurrences across 1 files
  CaptionFixedRightW48: { type: 'Text', size: 9, style: { width: 48, textAlign: 'right' } },

  // 2 occurrences across 2 files
  SectionG6P82: { type: 'Col', style: { gap: 6, padding: 8, borderBottomWidth: 1 } },

  // 2 occurrences across 2 files
  InlineG65s: { type: 'Row', style: {
      paddingHorizontal: 10,
      paddingVertical: 6,
      alignItems: 'center',
      gap: 6,
      borderBottomWidth: 1,
    } },

  // 2 occurrences across 2 files
  Box4pW100Pct3: { type: 'Col', style: {
      width: '100%',
      height: '100%',
      minHeight: 0,
      borderLeftWidth: 1,
    } },

  // 2 occurrences across 2 files
  BandG8P12: { type: 'Row', style: {
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 8,
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 10,
      paddingBottom: 10,
      borderBottomWidth: 1,
      flexWrap: 'wrap',
    } },

  // 2 occurrences across 2 files
  HalfV17: { type: 'Box', style: {
      flexGrow: 1,
      flexBasis: 0,
      minHeight: 0,
      position: 'relative',
    } },

  // 2 occurrences across 2 files
  Box7p: { type: 'Box', style: {
      position: 'absolute',
      right: 10,
      top: 10,
      bottom: 10,
      width: 250,
      borderWidth: 1,
      overflow: 'hidden',
    } },

  // 2 occurrences across 2 files
  SectionG4P8: { type: 'Col', style: { gap: 4, padding: 8 } },

  // 2 occurrences across 2 files
  Pad55s2: { type: 'Pressable', style: {
      paddingLeft: 5,
      paddingRight: 5,
      paddingTop: 2,
      paddingBottom: 2,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  InlineG23s2: { type: 'Row', style: { flexWrap: 'wrap', gap: 2, alignItems: 'flex-start' } },

  // 2 occurrences across 2 files
  InlineG8V14: { type: 'Row', style: { gap: 8, alignItems: 'flex-start' } },

  // 2 occurrences across 2 files
  Stack14W100Pct: { type: 'Col', style: { width: '100%', gap: 14 } },

  // 2 occurrences across 2 files
  TextPlain24: { type: 'Text', style: { fontStyle: 'italic' } },

  // 2 occurrences across 1 files
  InlineG24s: { type: 'Row', style: {
      flexWrap: 'wrap',
      gap: 2,
      alignItems: 'center',
      minWidth: 0,
    } },

  // 2 occurrences across 2 files
  TextPlain25: { type: 'Text', style: { whiteSpace: 'pre-wrap', minWidth: 0 } },

  // 2 occurrences across 2 files
  HalfV18: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  InlineV12: { type: 'Row', style: { borderBottomWidth: 1 } },

  // 2 occurrences across 1 files
  HalfP8: { type: 'Box', style: {
      flexGrow: 1,
      flexBasis: 0,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 7,
      paddingBottom: 7,
      minWidth: 90,
    } },

  // 2 occurrences across 2 files
  Label3s7: { type: 'Text', fontSize: 9, style: { fontWeight: 'bold', letterSpacing: 1 } },

  // 2 occurrences across 2 files
  Pad142s: { type: 'Box', style: { padding: 14, borderWidth: 1 } },

  // 2 occurrences across 2 files
  BandG8P102: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      padding: 10,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Box3pV17: { type: 'Box', style: { height: '100%', flexDirection: 'column', borderLeftWidth: 1 } },

  // 2 occurrences across 2 files
  SectionG4P102: { type: 'Col', style: { gap: 4, padding: 10, borderBottomWidth: 1 } },

  // 2 occurrences across 2 files
  TagR8: { type: 'Pressable', style: { padding: 4, borderRadius: 8, borderWidth: 1 } },

  // 2 occurrences across 2 files
  CardR12P12: { type: 'Box', style: {
      padding: 12,
      borderRadius: 12,
      borderWidth: 1,
      gap: 6,
    } },

  // 2 occurrences across 1 files
  SectionG6P122: { type: 'Box', style: { paddingLeft: 12, paddingRight: 12, gap: 6 } },

  // 2 occurrences across 1 files
  Pad124s2: { type: 'Box', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 14,
      paddingBottom: 8,
    } },

  // 2 occurrences across 2 files
  Pad125s: { type: 'Pressable', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  BandG8P103: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      flexWrap: 'wrap',
      padding: 10,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Pad10V11: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 5,
      paddingBottom: 5,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  BoldTextPlain19: { type: 'Text', fontSize: 8, style: { fontFamily: 'monospace', fontWeight: 'bold' } },

  // 2 occurrences across 2 files
  Box4pW100Pct4: { type: 'Box', style: {
      position: 'relative',
      width: '100%',
      height: '100%',
      overflow: 'visible',
    } },

  // 2 occurrences across 2 files
  BandG109s: { type: 'Row', style: {
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 10,
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 10,
      paddingBottom: 10,
      borderBottomWidth: 1,
      flexWrap: 'wrap',
    } },

  // 2 occurrences across 2 files
  Box5pW100Pct2: { type: 'Graph', style: {
      position: 'absolute',
      left: 0,
      top: 0,
      width: '100%',
      height: '100%',
    } },

  // 2 occurrences across 2 files
  Box3pV18: { type: 'Box', style: { position: 'absolute', left: 0, top: 0 } },

  // 2 occurrences across 2 files
  HalfP10: { type: 'Box', style: {
      flexGrow: 1,
      flexBasis: 0,
      minHeight: 0,
      padding: 10,
    } },

  // 2 occurrences across 2 files
  BareV12: { type: 'Box', style: { alignItems: 'center' } },

  // 2 occurrences across 1 files
  Box5pW20: { type: 'Box', style: {
      width: 20,
      height: 20,
      borderWidth: 1,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 2 files
  SectionG3: { type: 'Col', style: { padding: 4, gap: 3 } },

  // 2 occurrences across 1 files
  Box5pW22: { type: 'Box', style: {
      width: 22,
      height: 22,
      borderWidth: 1,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 2 files
  Box4pW340: { type: 'ScrollView', showScrollbar: true, style: { width: 340, flexShrink: 0, minHeight: 0 } },

  // 2 occurrences across 2 files
  Box5pV9: { type: 'Box', style: {
      position: 'absolute',
      left: 0,
      top: 0,
      right: 0,
      bottom: 0,
    } },

  // 2 occurrences across 2 files
  SpacerV15: { type: 'ScrollView', style: { flexGrow: 1, minHeight: 0 } },

  // 2 occurrences across 2 files
  Box3pV19: { type: 'Box', style: { paddingHorizontal: 6, paddingVertical: 2, borderWidth: 1 } },

  // 2 occurrences across 2 files
  Box3pV20: { type: 'Pressable', style: { paddingHorizontal: 12, paddingVertical: 7, borderWidth: 1 } },

  // 2 occurrences across 1 files
  InlineG8V15: { type: 'Row', style: { gap: 8, alignItems: 'flex-end', flexWrap: 'wrap' } },

  // 2 occurrences across 2 files
  InlineV13: { type: 'Row', style: { height: 18, alignItems: 'center' } },

  // 2 occurrences across 2 files
  HalfV21: { type: 'Row', style: { flexGrow: 1, flexShrink: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  Pad10V12: { type: 'ScrollView', style: { flexGrow: 1, padding: 10 } },

  // 2 occurrences across 2 files
  HalfV22: { type: 'Row', style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  InlineV14: { type: 'Row', style: { alignItems: 'center', justifyContent: 'space-between', borderBottomWidth: 1 } },

  // 2 occurrences across 2 files
  HalfGap8V2: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      flexGrow: 1,
      flexBasis: 0,
    } },

  // 2 occurrences across 2 files
  BandG6P84: { type: 'Pressable', style: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 6,
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 6,
      paddingBottom: 6,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Label4s: { type: 'Text', fontSize: 9, style: { fontFamily: 'monospace', fontWeight: 'bold', letterSpacing: 0.5 } },

  // 2 occurrences across 2 files
  Icon100Pctx100Pct: { type: 'Image', style: { width: '100%', height: '100%' } },

  // 2 occurrences across 2 files
  HalfP12: { type: 'ScrollView', showScrollbar: true, style: {
      flexGrow: 1,
      flexBasis: 0,
      minHeight: 0,
      padding: 12,
    } },

  // 2 occurrences across 2 files
  InlineV15: { type: 'Row', style: { alignItems: 'flex-end' } },

  // 2 occurrences across 2 files
  BareV13: { type: 'Box', style: { overflow: 'hidden' } },

  // 2 occurrences across 2 files
  CardR14P12: { type: 'Box', style: {
      padding: 12,
      gap: 12,
      borderRadius: 14,
      borderWidth: 1,
      borderColor: '#273142',
      backgroundColor: '#0f141c',
    } },

  // 2 occurrences across 2 files
  BoldTextCe6e: { type: 'Text', fontSize: 13, color: '#e6edf3', style: { fontWeight: 'bold' } },

  // 2 occurrences across 2 files
  TextC8b92: { type: 'Text', fontSize: 10, color: '#8b949e' },

  // 2 occurrences across 2 files
  CardR12P10: { type: 'Box', style: {
      width: 108,
      padding: 10,
      gap: 8,
      borderRadius: 12,
      borderWidth: 1,
      borderColor: '#243041',
      backgroundColor: '#121926',
    } },

  // 2 occurrences across 2 files
  RoundR8W32: { type: 'Box', style: {
      width: 32,
      height: 32,
      borderRadius: 8,
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#182233',
    } },

  // 2 occurrences across 2 files
  BoldTextCc9d: { type: 'Text', fontSize: 10, color: '#c9d1d9', style: { fontWeight: 'bold' } },

  // 2 occurrences across 2 files
  TextC8b93: { type: 'Text', fontSize: 9, color: '#8b949e' },

  // 2 occurrences across 2 files
  SectionG8P122: { type: 'Col', style: { gap: 8, padding: 12, borderWidth: 1 } },

  // 2 occurrences across 1 files
  TextFixedW44: { type: 'Text', fontSize: 10, style: { width: 44 } },

  // 2 occurrences across 1 files
  TextFixedW442: { type: 'Text', fontSize: 9, style: { width: 44 } },

  // 2 occurrences across 2 files
  TextPlain26: { type: 'Text', fontSize: 9, style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  HalfP122: { type: 'ScrollView', showScrollbar: true, style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 0,
      minHeight: 0,
      padding: 12,
    } },

  // 2 occurrences across 1 files
  HalfGap44s: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 260,
      gap: 4,
    } },

  // 2 occurrences across 1 files
  BadgeR12P102: { type: 'TextArea', fontSize: 10, style: {
      minHeight: 150,
      padding: 10,
      borderRadius: 12,
      borderWidth: 1,
      fontFamily: 'monospace',
    } },

  // 2 occurrences across 1 files
  TextPlain27: { type: 'Text', style: { fontFamily: 'monospace', whiteSpace: 'pre' } },

  // 2 occurrences across 2 files
  BareW100Pct: { type: 'Col', style: { width: '100%' } },

  // 2 occurrences across 2 files
  InlineG8V16: { type: 'Row', style: {
      justifyContent: 'space-between',
      alignItems: 'center',
      borderBottomWidth: 1,
      gap: 8,
    } },

  // 2 occurrences across 2 files
  Pad62s2: { type: 'Pressable', style: { padding: 6, borderWidth: 1 } },

  // 2 occurrences across 2 files
  HalfV25: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 0,
      borderWidth: 1,
      backgroundColor: '#0a0f17',
      overflow: 'hidden',
    } },

  // 2 occurrences across 2 files
  SpacerV16: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, height: '100%', backgroundColor: '#0a0f17' } },

  // 2 occurrences across 2 files
  Box2pV16: { type: 'Box', style: { position: 'relative', backgroundColor: '#0b1017' } },

  // 2 occurrences across 1 files
  TextPlain28: { type: 'Text', fontSize: 8, style: { textTransform: 'uppercase', letterSpacing: 0.8 } },

  // 2 occurrences across 1 files
  SpacerV17: { type: 'Box', style: { flexGrow: 1, flexBasis: 180 } },

  // 2 occurrences across 2 files
  SpacerG10: { type: 'Col', style: { gap: 10, flexGrow: 1, minHeight: 0 } },

  // 2 occurrences across 2 files
  SectionG4P12: { type: 'Box', style: { padding: 12, borderWidth: 1, gap: 4 } },

  // 2 occurrences across 1 files
  InlineG8V17: { type: 'Row', style: { justifyContent: 'space-between', gap: 8 } },

  // 2 occurrences across 2 files
  MedTextPlain5: { type: 'Text', size: 13 },

  // 2 occurrences across 1 files
  InlineG32: { type: 'Row', style: { gap: 32 } },

  // 2 occurrences across 2 files
  SpacerG6: { type: 'Row', style: { alignItems: 'center', gap: 6, flexGrow: 1 } },

  // 2 occurrences across 2 files
  CaptionFixedRightW90: { type: 'Text', size: 9, style: { width: 90, textAlign: 'right' } },

  // 2 occurrences across 2 files
  SpacerV20: { type: 'Pressable', style: { flexGrow: 1 } },

  // 2 occurrences across 2 files
  BareV15: { type: 'ScrollView', style: { maxHeight: 240 } },

  // 2 occurrences across 2 files
  CardR6G62: { type: 'Row', style: {
      padding: 8,
      borderRadius: 6,
      gap: 6,
      alignItems: 'center',
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  RoundR3: { type: 'Pressable', style: {
      paddingHorizontal: 6,
      paddingVertical: 3,
      borderRadius: 3,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Body11Plain7: { type: 'Text', size: 11, style: { padding: 10 } },

  // 2 occurrences across 1 files
  CaptionFixedRightW60: { type: 'Text', size: 9, style: { width: 60, textAlign: 'right' } },

  // 2 occurrences across 2 files
  InlineG8V18: { type: 'Row', style: { alignItems: 'baseline', gap: 8 } },

  // 2 occurrences across 1 files
  HalfV26: { type: 'Col', style: {
      flexGrow: 1,
      flexShrink: 1,
      flexBasis: 0,
      borderRightWidth: 1,
    } },

  // 2 occurrences across 2 files
  Box4pV8: { type: 'Pressable', style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 0,
    } },

  // 2 occurrences across 2 files
  Box4pV9: { type: 'Pressable', style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
    } },

  // 2 occurrences across 2 files
  TinyPlain4: { type: 'Text', size: 8 },

  // 2 occurrences across 1 files
  CaptionPlain9: { type: 'Text', size: 9, style: { flexGrow: 1 } },

  // 2 occurrences across 1 files
  Body11Plain8: { type: 'Text', size: 11 },

  // 2 occurrences across 2 files
  Box3pV21: { type: 'Pressable', style: { paddingHorizontal: 10, paddingVertical: 3, borderWidth: 1 } },

  // 2 occurrences across 1 files
  TextFixedW30: { type: 'Text', style: { width: 30 } },

  // 2 occurrences across 2 files
  Dot84s: { type: 'Box', style: {
      width: 8,
      height: 8,
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  TextFixedW70: { type: 'Text', style: { width: 70 } },

  // 2 occurrences across 2 files
  TextFixedW120: { type: 'Text', style: { width: 120 } },

  // 2 occurrences across 2 files
  TextFixedW90: { type: 'Text', style: { width: 90 } },

  // 2 occurrences across 2 files
  BandG8P8: { type: 'Row', style: {
      alignItems: 'center',
      gap: 8,
      flexWrap: 'wrap',
      padding: 8,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  SectionG10P102: { type: 'Col', style: {
      width: '100%',
      height: '100%',
      padding: 10,
      gap: 10,
    } },

  // 2 occurrences across 1 files
  Box5pW18: { type: 'Pressable', style: {
      width: 18,
      height: 18,
      borderWidth: 1,
      justifyContent: 'center',
      alignItems: 'center',
    } },

  // 2 occurrences across 1 files
  TextFixedCenterW30: { type: 'Text', style: { width: 30, textAlign: 'center' } },

  // 2 occurrences across 1 files
  TextFixedCenterW40: { type: 'Text', style: { width: 40, textAlign: 'center' } },

  // 2 occurrences across 2 files
  Pad8V12: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 3,
      paddingBottom: 3,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  SectionG8P105: { type: 'Col', style: { gap: 8, padding: 10, borderTopWidth: 1 } },

  // 2 occurrences across 2 files
  Pad65s4: { type: 'Pressable', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Pad54s: { type: 'Box', style: {
      paddingLeft: 5,
      paddingRight: 5,
      paddingTop: 1,
      paddingBottom: 1,
    } },

  // 2 occurrences across 2 files
  Pad10V13: { type: 'Box', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 4,
      paddingBottom: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  LargeTextPlain: { type: 'Text', size: 18 },

  // 2 occurrences across 1 files
  Body11C5c6: { type: 'Text', size: 11, color: '#5c6a78' },

  // 2 occurrences across 1 files
  Box4pW1: { type: 'Box', style: {
      width: 1,
      height: 22,
      backgroundColor: '#1a222c',
      marginHorizontal: 6,
    } },

  // 2 occurrences across 1 files
  RoundR6V2: { type: 'Pressable', style: {
      paddingHorizontal: 10,
      paddingVertical: 5,
      borderRadius: 6,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Body11Ce6e: { type: 'Text', size: 11, color: '#e6edf3' },

  // 2 occurrences across 2 files
  BodyC8b9: { type: 'Text', size: 10, color: '#8b98a6', style: { letterSpacing: 1 } },

  // 2 occurrences across 2 files
  BodyC5c62: { type: 'Text', size: 10, color: '#5c6a78', style: { letterSpacing: 1 } },

  // 2 occurrences across 2 files
  InlineG22s: { type: 'Row', style: { gap: 2, alignItems: 'center' } },

  // 2 occurrences across 2 files
  InlineG63s4: { type: 'Row', style: { gap: 6, flexWrap: 'wrap', marginTop: 4 } },

  // 2 occurrences across 1 files
  Box4pW12: { type: 'Box', style: {
      width: 1,
      height: 20,
      backgroundColor: '#1a222c',
      marginHorizontal: 6,
    } },

  // 2 occurrences across 1 files
  TextPlain29: { type: 'Text', style: { fontWeight: 700 } },

  // 2 occurrences across 2 files
  Box2pV17: { type: 'Box', style: { overflow: 'hidden', position: 'relative' } },

  // 2 occurrences across 1 files
  Pad125s2: { type: 'Pressable', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 6,
      paddingBottom: 6,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  RoundR9: { type: 'Box', style: {
      width: 18,
      height: 18,
      borderRadius: 9,
      justifyContent: 'center',
      alignItems: 'center',
    } },

  // 2 occurrences across 2 files
  BoldTextC0002: { type: 'Text', fontSize: 7, color: '#000', style: { fontWeight: 'bold' } },

  // 2 occurrences across 2 files
  Stack6V6: { type: 'Col', style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 46,
      gap: 6,
      maxHeight: 280,
      overflow: 'scroll',
      zIndex: 10000,
    } },

  // 2 occurrences across 1 files
  Box3pW10: { type: 'Box', style: { width: 10, height: 10, borderWidth: 1 } },

  // 2 occurrences across 1 files
  Pad125s3: { type: 'Pressable', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 8,
      paddingBottom: 8,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Pad10V14: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 8,
      paddingBottom: 8,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Pad143s: { type: 'Box', style: { padding: 14, borderWidth: 1, alignItems: 'center' } },

  // 2 occurrences across 2 files
  SectionG8P14: { type: 'Box', style: { padding: 14, borderWidth: 1, gap: 8 } },

  // 2 occurrences across 2 files
  SectionG6P14: { type: 'Box', style: { padding: 14, borderWidth: 1, gap: 6 } },

  // 2 occurrences across 1 files
  Pad8H30: { type: 'TextInput', style: {
      height: 30,
      borderWidth: 1,
      paddingLeft: 8,
      fontFamily: 'monospace',
    } },

  // 2 occurrences across 2 files
  InlineG103s5: { type: 'Row', style: { gap: 10, flexWrap: 'wrap', overflow: 'visible' } },

  // 2 occurrences across 2 files
  Stack8V4: { type: 'Col', style: { gap: 8, overflow: 'visible' } },

  // 2 occurrences across 2 files
  Stack16: { type: 'Col', style: { gap: 16 } },

  // 2 occurrences across 2 files
  Stack14V3: { type: 'Box', style: { gap: 14, alignItems: 'flex-start' } },

  // 2 occurrences across 2 files
  HalfGap14: { type: 'Col', style: { flexGrow: 1, flexBasis: 0, gap: 14 } },

  // 2 occurrences across 2 files
  SectionG12P122: { type: 'Col', style: { padding: 12, gap: 12 } },

  // 2 occurrences across 1 files
  Box2pW14: { type: 'Box', style: { width: 14, height: 16 } },

  // 2 occurrences across 1 files
  Box5pW202: { type: 'Pressable', style: {
      width: 20,
      height: 20,
      borderWidth: 1,
      justifyContent: 'center',
      alignItems: 'center',
    } },

  // 2 occurrences across 2 files
  SectionG8P106: { type: 'Col', style: { gap: 8, padding: 10, borderBottomWidth: 1 } },

  // 2 occurrences across 2 files
  TextPlain30: { type: 'Text', style: { padding: 4 } },

  // 2 occurrences across 2 files
  TextPlain31: { type: 'Text', style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  Box4pW22: { type: 'Box', style: {
      width: 22,
      height: 22,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 2 files
  Label2s: { type: 'Text', style: { fontWeight: 'bold', letterSpacing: 0.5 } },

  // 2 occurrences across 2 files
  Pad65s5: { type: 'Box', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 1,
      paddingBottom: 1,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  HalfGap10P12: { type: 'Col', style: {
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 0,
      gap: 10,
      padding: 12,
    } },

  // 2 occurrences across 2 files
  InlineG63s5: { type: 'Row', style: { gap: 6, alignItems: 'center', minWidth: 0 } },

  // 2 occurrences across 2 files
  RoundR7: { type: 'Box', style: {
      position: 'absolute',
      top: 2,
      width: 14,
      height: 14,
      marginLeft: -7,
      borderRadius: 7,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  SectionG10P103: { type: 'Col', style: { padding: 10, gap: 10, borderBottomWidth: 1 } },

  // 2 occurrences across 1 files
  Box4pV13: { type: 'Box', style: {
      flexShrink: 1,
      flexBasis: 0,
      minWidth: 0,
      minHeight: 0,
    } },

  // 2 occurrences across 2 files
  Box6p: { type: 'Box', style: {
      position: 'absolute',
      left: 0,
      top: 0,
      right: 0,
      bottom: 0,
      pointerEvents: 'none',
    } },

  // 2 occurrences across 2 files
  HalfGap24s3: { type: 'Col', style: {
      gap: 2,
      flexGrow: 1,
      flexBasis: 0,
      minWidth: 180,
    } },

  // 2 occurrences across 1 files
  ChipR8P84: { type: 'TextInput', style: {
      height: 34,
      borderWidth: 1,
      borderRadius: 8,
      paddingLeft: 8,
    } },

  // 2 occurrences across 1 files
  HalfGap6V2: { type: 'Row', style: {
      gap: 6,
      alignItems: 'center',
      flexWrap: 'wrap',
      flexGrow: 1,
      flexBasis: 0,
    } },

  // 2 occurrences across 2 files
  Box2pV19: { type: 'Col', style: { borderTopWidth: 1, minHeight: 0 } },

  // 2 occurrences across 1 files
  SectionG10P104: { type: 'Col', style: {
      width: '100%',
      height: '100%',
      padding: 10,
      gap: 10,
      minHeight: 0,
    } },

  // 2 occurrences across 1 files
  SpacerV21: { type: 'ScrollView', showScrollbar: true, style: { flexGrow: 1, minHeight: 0 } },

  // 2 occurrences across 1 files
  Pad10V15: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 7,
      paddingBottom: 7,
    } },

  // 2 occurrences across 2 files
  BoldTextPlain20: { type: 'Text', fontSize: 10, style: { fontWeight: 'bold', flexShrink: 1 } },

  // 2 occurrences across 2 files
  BadgeR8P102: { type: 'Pressable', style: {
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
      borderRadius: 8,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  Pad122s2: { type: 'Box', style: { padding: 12, borderBottomWidth: 1 } },

  // 2 occurrences across 2 files
  SectionG4P82: { type: 'Box', style: { gap: 4, padding: 8, borderWidth: 1 } },

  // 2 occurrences across 1 files
  Pad8V14: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 6,
      paddingBottom: 6,
    } },

  // 2 occurrences across 2 files
  SectionG4P103: { type: 'Col', style: { gap: 4, padding: 10 } },

  // 2 occurrences across 2 files
  Box2pV20: { type: 'Col', style: { height: '100%', borderLeftWidth: 1 } },

  // 2 occurrences across 2 files
  RoundR16: { type: 'Box', style: { borderWidth: 1, borderRadius: 16, overflow: 'hidden' } },

  // 2 occurrences across 2 files
  SectionG12P123: { type: 'Col', style: {
      width: '100%',
      height: '100%',
      minHeight: 0,
      gap: 12,
      padding: 12,
    } },

  // 2 occurrences across 1 files
  ChipR999V2: { type: 'Box', style: {
      paddingLeft: 8,
      paddingRight: 8,
      paddingTop: 5,
      paddingBottom: 5,
      borderRadius: 999,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  HalfGap10V3: { type: 'Row', style: {
      gap: 10,
      alignItems: 'center',
      flexGrow: 1,
      flexBasis: 0,
    } },

  // 2 occurrences across 2 files
  TextPlain32: { type: 'Text', fontSize: 10, numberOfLines: 1, style: { flexGrow: 1, flexBasis: 0 } },

  // 2 occurrences across 2 files
  InlineP10: { type: 'Row', style: {
      justifyContent: 'space-between',
      alignItems: 'center',
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
      borderBottomWidth: 1,
    } },

  // 2 occurrences across 2 files
  Label3s8: { type: 'Text', fontSize: 10, style: { fontWeight: 'bold', letterSpacing: 1 } },

  // 2 occurrences across 2 files
  Box3pV23: { type: 'TextInput', fontSize: 11, style: { borderWidth: 0, backgroundColor: 'transparent' } },

  // 2 occurrences across 2 files
  Pad145s: { type: 'Pressable', style: {
      paddingLeft: 14,
      paddingRight: 14,
      paddingTop: 10,
      paddingBottom: 10,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Box5pW26: { type: 'Pressable', style: {
      width: 26,
      height: 26,
      borderWidth: 1,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 1 files
  Pad8H26: { type: 'Pressable', style: {
      paddingLeft: 8,
      paddingRight: 8,
      height: 26,
      borderWidth: 1,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 1 files
  Pad125s4: { type: 'Pressable', style: {
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 10,
      paddingBottom: 10,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Pad64s: { type: 'Pressable', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 3,
      paddingBottom: 3,
    } },

  // 2 occurrences across 1 files
  Pad127s: { type: 'Box', style: {
      flex: 1,
      minWidth: 0,
      borderWidth: 1,
      paddingLeft: 12,
      paddingRight: 12,
      paddingTop: 9,
      paddingBottom: 9,
    } },

  // 2 occurrences across 1 files
  SectionG10P20: { type: 'Col', style: {
      flex: 1,
      minWidth: 0,
      gap: 10,
      padding: 20,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  SpacerG43: { type: 'Col', style: { gap: 4, flexGrow: 1 } },

  // 2 occurrences across 1 files
  Box3pV24: { type: 'Box', style: { justifyContent: 'center', alignItems: 'center', borderWidth: 1 } },

  // 2 occurrences across 1 files
  RoundR4W18: { type: 'Box', style: {
      width: 18,
      height: 18,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 4,
      borderWidth: 1,
    } },

  // 2 occurrences across 2 files
  BandG8P104: { type: 'Pressable', style: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 8,
      paddingLeft: 10,
      paddingRight: 10,
      paddingTop: 6,
      paddingBottom: 6,
    } },

  // 2 occurrences across 2 files
  InlineV17: { type: 'Row', style: { alignItems: 'center', flexShrink: 0 } },

  // 2 occurrences across 2 files
  BandG5: { type: 'Pressable', style: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 5,
      paddingTop: 3,
      paddingBottom: 3,
    } },

  // 2 occurrences across 1 files
  InlineG43s3: { type: 'Pressable', style: { flexDirection: 'row', alignItems: 'center', gap: 4 } },

  // 2 occurrences across 1 files
  RoundR2W102: { type: 'Box', style: {
      width: 10,
      height: 10,
      borderRadius: 2,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  Box2pW420: { type: 'Col', style: { width: 420, gap: 0 } },

  // 2 occurrences across 1 files
  HalfV27: { type: 'Col', style: { flexGrow: 1, flexBasis: 0, minWidth: 0 } },

  // 2 occurrences across 1 files
  ChipR999H22: { type: 'Pressable', style: {
      height: 22,
      paddingLeft: 8,
      paddingRight: 8,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 999,
      borderWidth: 1,
    } },

  // 2 occurrences across 1 files
  BadgeR8P12: { type: 'Box', style: { padding: 12, borderRadius: 8, borderWidth: 1 } },

  // 2 occurrences across 2 files
  Box3pW24: { type: 'Pressable', style: { opacity: 0, position: 'absolute', width: 24 } },

  // 2 occurrences across 2 files
  InlineG31s: { type: 'Row', style: { gap: 3 } },

  // 2 occurrences across 2 files
  InlineG9: { type: 'Row', style: { gap: 9, alignItems: 'center' } },

  // 2 occurrences across 2 files
  RoundR99: { type: 'Box', style: { borderRadius: 99 } },

  // 2 occurrences across 2 files
  BareV16: { type: 'Box', style: { height: 6 } },

  // 2 occurrences across 1 files
  TinyC9eb: { type: 'Text', size: 8, color: '#9eb4ff', style: { fontFamily: 'monospace' } },

  // 2 occurrences across 2 files
  Box3pW20: { type: 'Pressable', style: { opacity: 0, position: 'absolute', width: 20 } },

  // 2 occurrences across 1 files
  Box3pV27: { type: 'Box', style: { position: 'absolute', backgroundColor: '#cf6d40', opacity: 0.94 } },

  // 2 occurrences across 2 files
  Box4pW32: { type: 'Pressable', style: {
      opacity: 0,
      position: 'absolute',
      width: 32,
      height: 32,
    } },

  // 2 occurrences across 2 files
  Box2pV22: { type: 'Box', style: { position: 'absolute', alignItems: 'center' } },

  // 2 occurrences across 2 files
  Box5pW10: { type: 'Box', style: {
      width: 10,
      height: 10,
      borderWidth: 1,
      alignItems: 'center',
      justifyContent: 'center',
    } },

  // 2 occurrences across 2 files
  Pad64s2: { type: 'Box', style: {
      paddingLeft: 6,
      paddingRight: 6,
      paddingTop: 1,
      paddingBottom: 1,
    } },

  // 2 occurrences across 2 files
  TinyPlain5: { type: 'Text', size: 8, style: { letterSpacing: 1.2 } },

  // 2 occurrences across 1 files
  BoldBodyFixedCenter: { type: 'Text', size: 11, bold: true, style: { width: 14, textAlign: 'center' } },

  // 2 occurrences across 2 files
  Divider5s: { type: 'Box', style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: 7,
      height: 1,
    } },

  // 2 occurrences across 1 files
  InlineV18: { type: 'Row', style: { width: '100%', justifyContent: 'space-between' } },

  // 2 occurrences across 2 files
  BareW1: { type: 'Box', style: { width: 1 } },

  // 2 occurrences across 1 files
  Label4s2: { type: 'Text', size: 9, bold: true, style: { letterSpacing: 1.1, textTransform: 'uppercase' } },

  // 2 occurrences across 2 files
  Stack8V5: { type: 'Col', style: { alignItems: 'center', gap: 8 } },

  // 2 occurrences across 2 files
  Body11Plain9: { type: 'Text', size: 11, style: { fontFamily: 'monospace' } },

  // 2 occurrences across 1 files
  InlineG8W100Pct3: { type: 'Row', style: { width: '100%', gap: 8, flexWrap: 'wrap' } },

  // 2 occurrences across 2 files
  Stack10V3: { type: 'Col', style: { gap: 10, alignItems: 'center' } },

  // 2 occurrences across 1 files
  InlineG22: { type: 'Row', style: {
      width: '100%',
      flexWrap: 'wrap',
      gap: 22,
      alignItems: 'flex-start',
      justifyContent: 'center',
    } },

});
