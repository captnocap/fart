-- generated by eqjs_transpiler
local exports = {}

local function mix(a, b)
  local total = a + b
  local nums = eqjs:typed_array("double", 3)
  nums[0] = 1.0
  nums[1] = 2.0
  nums[2] = 3.0
  local idx = 0
  local acc = 0
  while idx < 3 do
    acc = acc + nums[idx]
    idx = idx + 1
  end
  return acc + total
end
exports.mix = mix
local function main()
  return mix(2, 3)
end
exports.main = main

return { exports = exports }
