/**
 * Minimap — generated by scripts/tslx_compile.mjs.
 *
 * Bulk-rendering primitive: one React host node that the framework
 * paints natively in Zig from data-in props. Do NOT pass children.
 *
 * Props:
 *   rows
 *   rowHeight
 *   rowGap
 *   activeColor
 *   inactiveColor
 */
const React: any = require('react');
export const Minimap: any = (props: any) => React.createElement('Minimap', props);
