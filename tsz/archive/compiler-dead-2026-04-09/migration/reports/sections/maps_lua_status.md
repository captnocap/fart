# Maps Lua Status — Contract 0341-0370

## Grounding Line

a029 wrapper registration; a030 rebuilder functions; a031 nested helper; a032 master dispatch; lua_tree required

## Atom Results

- a029 lua_map_wrapper_registration: MATCH — scans __lmwN tags, emits luajit_runtime.setMapWrapper() pointers
- a030 lua_map_rebuilder_functions: MATCH — emits __rebuildLuaMapN() per rebuilder with empty-items branch and __declareChildren(wrapper, tmpl)
- a031 lua_nested_helpers: MATCH — documents __luaNestedMap(arr, fn) shape exactly as specified
- a032 lua_map_master_dispatch: MATCH — emits __rebuildLuaMaps() dispatching each __rebuildLuaMapN() in order

## Byte Parity

- maps_lua_rebuilder.json (d01_nested_maps.tsz):
  - legacy_hash: 2de77e1f93384606b1c204840f1961e55a6bdf096b4006cdb510eb6c1352e2bb
  - atom_hash: 8d17d52b4d4a782d09004794aeaf4d1ec280d4d30fb674cfce24e40de9ddd67b
  - lane: mixed
  - backend_tags: lua_map, nested_map, handlers
  - split_output: true (app.zig, handlers.zig, logic.zig, maps.zig, nodes.zig, state.zig)
  - diff_status: DIFF — first diff is preamble: legacy starts `const std = @import("std")` vs atom starts `//! Generated by tsz compiler (Zig)`
- maps_lua_master.json (d07_sibling_maps_shared_state.tsz):
  - legacy_hash: a094380ca6851c3c1dd9d429ccdf9cf365ce9270ebc11b55a82989b561dfaef1
  - atom_hash: 98d390f7bdaddcbe9cd3a5df2d1235907fd3f3097f203930619d689795a6d561
  - lane: mixed
  - backend_tags: lua_map, handlers
  - split_output: true (app.zig, handlers.zig, logic.zig, maps.zig, nodes.zig, state.zig)
  - diff_status: DIFF — same preamble difference (atom has doc-comment banner, legacy does not)
- FIXED (atoms patched): Zero maps-lua diffs in both carts
  - Fixes applied to a029, a030, a031, a032:
    1. a030/a031/a032: return lines.join('\\n') instead of array (fixes comma-delimited formatting)
    2. a030: removed [a030] debug comments
    3. a030/a032: skip null-body rebuilders (fixes spurious __rebuildLuaMap0)
    4. a029: removed nodes. prefix, added _appInit indentation + comment, skips null-body maps
  - d01_nested_maps: 0 map-related lines in diff (was 4 categories of diffs)
  - d07_sibling_maps_shared_state: 0 map-related lines in diff
  - Remaining diffs are all non-maps: a004 dash length, a005 _initState, dyn_text init, runtime entrypoint structure, state exports
  - Diff reduced from 332 to 133 lines for d01 (60% reduction)
- Comparison mode: pre-split (0471f002), lua-tree suppressed (39bae560)
- predicted_atoms: [] — route plan integration pending

## Visual Parity

- maps_lua_rebuilder_visual.json: BLOCKED — visual capture harness missing
- maps_lua_master_visual.json: BLOCKED — visual capture harness missing
- Region hashes: wrapper_row, lua_map_body (rebuilder); wrapper_row, rendered_children (master) — not capturable
- Blocker recorded: 0341-0370 maps-lua visual harness missing

## Behavioral Parity

Behavioral parity is required for Lua maps because wrapper registration and master dispatch affect runtime reload behavior.
Behavioral parity not yet verified — harness infrastructure required for runtime reload + dispatch testing.
