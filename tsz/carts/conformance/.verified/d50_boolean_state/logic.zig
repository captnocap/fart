//! Generated by tsz compiler — d50_boolean_state [logic.zig]
//! Source: d50_boolean_state.tsz


// ── Embedded JS logic ──
pub const JS_LOGIC =
    \\var __luaReady = false;
    \\var darkMode = 0;
    \\function setDarkMode(v) { darkMode = v; if (__luaReady) __luaEval("setDarkMode(" + v + ")"); else __markDirty(); }
    \\var showSidebar = 1;
    \\function setShowSidebar(v) { showSidebar = v; if (__luaReady) __luaEval("setShowSidebar(" + v + ")"); else __markDirty(); }
    \\var showFooter = 1;
    \\function setShowFooter(v) { showFooter = v; if (__luaReady) __luaEval("setShowFooter(" + v + ")"); else __markDirty(); }
    \\var locked = 0;
    \\function setLocked(v) { locked = v; if (__luaReady) __luaEval("setLocked(" + v + ")"); else __markDirty(); }
    \\var expanded = 0;
    \\function setExpanded(v) { expanded = v; if (__luaReady) __luaEval("setExpanded(" + v + ")"); else __markDirty(); }
    \\var toggleCount = 0;
    \\function setToggleCount(v) { toggleCount = v; if (__luaReady) __luaEval("setToggleCount(" + v + ")"); else __markDirty(); }
    \\
;

// ── Embedded Lua logic ──
pub const LUA_LOGIC =
    \\local _state = {}
    \\function _getState(k) return _state[k] end
    \\function _setState(k, v) _state[k] = v; __markDirty() end
    \\
    \\function __mapLoop(arr, fn)
    \\  if not arr then return nil end
    \\  local r = {}
    \\  for i, item in ipairs(arr) do
    \\    r[#r + 1] = fn(item, i)
    \\  end
    \\  r.__isMapResult = true
    \\  return r
    \\end
    \\
    \\function __flattenChildren(children)
    \\  if not children then return {} end
    \\  local flat = {}
    \\  for _, child in ipairs(children) do
    \\    if type(child) == "table" and child.__isMapResult then
    \\      for _, mc in ipairs(child) do flat[#flat + 1] = mc end
    \\    elseif child ~= nil then
    \\      flat[#flat + 1] = child
    \\    end
    \\  end
    \\  return flat
    \\end
    \\
    \\_state["darkMode"] = 0
    \\darkMode = _state["darkMode"]
    \\function setDarkMode(v) _state["darkMode"] = v; darkMode = v; __markDirty() end
    \\_state["showSidebar"] = 1
    \\showSidebar = _state["showSidebar"]
    \\function setShowSidebar(v) _state["showSidebar"] = v; showSidebar = v; __markDirty() end
    \\_state["showFooter"] = 1
    \\showFooter = _state["showFooter"]
    \\function setShowFooter(v) _state["showFooter"] = v; showFooter = v; __markDirty() end
    \\_state["locked"] = 0
    \\locked = _state["locked"]
    \\function setLocked(v) _state["locked"] = v; locked = v; __markDirty() end
    \\_state["expanded"] = 0
    \\expanded = _state["expanded"]
    \\function setExpanded(v) _state["expanded"] = v; expanded = v; __markDirty() end
    \\_state["toggleCount"] = 0
    \\toggleCount = _state["toggleCount"]
    \\function setToggleCount(v) _state["toggleCount"] = v; toggleCount = v; __markDirty() end
    \\
    \\function App()
    \\  return { style = { background_color = (darkMode == 1) and 0x0f172a or 0xf8fafc, padding = 20, width = -1, height = -1, gap = 12, overflow = "hidden" }, children = {
    \\    { text = "Boolean State", font_size = 16, text_color = (darkMode == 1) and 0xe2e8f0 or 0x1e293b },
    \\    { text = "toggles: " .. tostring(toggleCount), font_size = 10, text_color = (darkMode == 1) and 0x64748b or 0x94a3b8 },
    \\    { style = { flex_direction = "row", gap = 6, flex_wrap = "wrap" }, children = {
    \\      { style = { padding = 8, background_color = (darkMode == 1) and 0x334155 or 0xe2e8f0, border_radius = 6 }, lua_on_press = "setDarkMode(darkMode == 0 and 1 or 0); setToggleCount(toggleCount + 1)", children = {
    \\        { text = (darkMode == 1) and "Light Mode" or "Dark Mode", font_size = 11, text_color = (darkMode == 1) and 0xe2e8f0 or 0x1e293b }
    \\      } },
    \\      { style = { padding = 8, background_color = 0x334155, border_radius = 6 }, lua_on_press = "setShowSidebar(showSidebar == 0 and 1 or 0); setToggleCount(toggleCount + 1)", children = {
    \\        { text = (showSidebar == 1) and "Hide Sidebar" or "Show Sidebar", font_size = 11, text_color = 0xe2e8f0 }
    \\      } },
    \\      { style = { padding = 8, background_color = 0x334155, border_radius = 6 }, lua_on_press = "setShowFooter(showFooter == 0 and 1 or 0); setToggleCount(toggleCount + 1)", children = {
    \\        { text = (showFooter == 1) and "Hide Footer" or "Show Footer", font_size = 11, text_color = 0xe2e8f0 }
    \\      } },
    \\      { style = { padding = 8, background_color = (locked == 1) and 0xdc2626 or 0x334155, border_radius = 6 }, lua_on_press = "setLocked(locked == 0 and 1 or 0); setToggleCount(toggleCount + 1)", children = {
    \\        { text = (locked == 1) and "Unlock" or "Lock", font_size = 11, text_color = 0xffffff }
    \\      } },
    \\      { style = { padding = 8, background_color = 0x334155, border_radius = 6 }, lua_on_press = "setExpanded(expanded == 0 and 1 or 0); setToggleCount(toggleCount + 1)", children = {
    \\        { text = (expanded == 1) and "Collapse" or "Expand", font_size = 11, text_color = 0xe2e8f0 }
    \\      } }
    \\    } },
    \\    { style = { flex_direction = "row", gap = 8, flex_grow = 1, overflow = "auto" }, children = {
    \\      (showSidebar == 1) and { style = { width = 120, background_color = (darkMode == 1) and 0x1e293b or 0xe2e8f0, border_radius = 8, padding = 12, gap = 4 }, children = {
    \\        { text = "Sidebar", font_size = 12, text_color = (darkMode == 1) and 0xe2e8f0 or 0x1e293b },
    \\        { text = "Nav item 1", font_size = 10, text_color = (darkMode == 1) and 0x64748b or 0x94a3b8 },
    \\        { text = "Nav item 2", font_size = 10, text_color = (darkMode == 1) and 0x64748b or 0x94a3b8 },
    \\        { text = "Nav item 3", font_size = 10, text_color = (darkMode == 1) and 0x64748b or 0x94a3b8 }
    \\      } } or nil,
    \\      { style = { flex_grow = 1, background_color = (darkMode == 1) and 0x1e293b or 0xffffff, border_radius = 8, padding = 12, gap = 8, overflow = "auto" }, children = {
    \\        { text = "Main Content", font_size = 13, text_color = (darkMode == 1) and 0xe2e8f0 or 0x1e293b },
    \\        (locked == 1) and { text = "Content is locked", font_size = 10, text_color = 0xef4444 } or nil,
    \\        (locked == 0) and { style = { gap = 4 }, children = {
    \\          { text = "Expanded detail line 1", font_size = 10, text_color = (darkMode == 1) and 0x94a3b8 or 0x64748b },
    \\          { text = "Expanded detail line 2", font_size = 10, text_color = (darkMode == 1) and 0x94a3b8 or 0x64748b },
    \\          { text = "Expanded detail line 3", font_size = 10, text_color = (darkMode == 1) and 0x94a3b8 or 0x64748b }
    \\        } } or nil,
    \\        (locked == 0) and { text = "Click expand for details", font_size = 10, text_color = (darkMode == 1) and 0x94a3b8 or 0x64748b } or nil
    \\      } }
    \\    } },
    \\    (showFooter == 1) and { style = { padding = 8, background_color = (darkMode == 1) and 0x1e293b or 0xe2e8f0, border_radius = 6 }, children = {
    \\      { text = "dark:" .. tostring(darkMode) .. " sidebar:" .. tostring(showSidebar) .. " footer:" .. tostring(showFooter) .. " locked:" .. tostring(locked) .. " expanded:" .. tostring(expanded), font_size = 9, text_color = (darkMode == 1) and 0x64748b or 0x94a3b8 }
    \\    } } or nil
    \\  } }
    \\end
    \\
    \\function __render()
    \\  __clearLuaNodes()
    \\  local tree = App()
    \\  if __mw0 then __declareChildren(__mw0, { tree }) end
    \\end
    \\
;

