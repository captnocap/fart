//! Generated by Smith (mod mode) — do not edit
//! Source: m11_upper_enums.mod.tsz
//! Module: http

const std = @import("std");

pub const Method = enum {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
};

pub const Status = enum {
    OK,
    NOT_FOUND,
    SERVER_ERROR,
    REDIRECT,
};

pub const Request = struct {
    method: Method = .GET,
    status: Status = .OK,
    retries: u8 = 3,
};

var current: Request = .{};
var last_status: Status = .OK;

pub fn set_method(m: Method) void {
    current.method = m;
}

pub fn is_error() bool {
    return last_status == .SERVER_ERROR;
}

pub fn is_redirect() bool {
    return last_status == .REDIRECT;
}

pub fn reset() void {
    current = .{ .method = .GET, .status = .OK, .retries = 3 };
    last_status = .OK;
}
