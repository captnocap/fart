//! Generated by Smith (mod mode) — do not edit
//! Source: m18_posix_constants.mod.tsz
//! Module: socket

const std = @import("std");
const posix = std.posix;

pub const Proto = enum {
    tcp,
    udp,
};

var fd: i32 = -1;

pub fn open_tcp() !i32 {
    fd = try posix.socket(posix.AF.INET, posix.SOCK.STREAM, 0);
    return fd;
}

pub fn open_udp() !i32 {
    fd = try posix.socket(posix.AF.INET, posix.SOCK.DGRAM, posix.IPPROTO.UDP);
    return fd;
}

pub fn shutdown() void {
    if (fd == -1) return;
    posix.close(fd);
    fd = -1;
}
