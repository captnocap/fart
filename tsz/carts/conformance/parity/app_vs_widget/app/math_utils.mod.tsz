<math_utils module>
  <functions>
    add(a, b):
      a + b

    multiply(a, b):
      a * b

    clamp(val, lo, hi):
      <if val below lo>
        lo
      </if>
      <else if val above hi>
        hi
      </else>
      <else>
        val
      </else>
  </functions>
</math_utils>
