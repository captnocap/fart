# Entry / Split / Finalize Status

Timestamp: 2026-04-09T00:00:00-07:00
Step Range: 0411-0458
Changed Files: tsz/compiler/smith/emit_atoms/entry/a039_app_init.js, tsz/compiler/smith/emit_atoms/entry/a040_app_tick.js, tsz/compiler/migration/reports/sections/entry_split_status.md, tsz/compiler/migration/reports/parity/entry_smoke.json, tsz/compiler/migration/reports/split/split_smoke.md, tsz/compiler/migration/state/blocked.txt
Verification Status: blocked

Grounding: a039 app_init; a040 app_tick; a041 app_exports; a042 app_main; a043 split section extraction; a044 split namespace prefixing; a045 split module headers; a046 finalize postpass. evalLuaMapData gap and luajit OA registration gap are the primary known defects.

a039_has_luajit_registerHostFn_for_oa_before_patch: false
a039_has_luajit_registerHostFn_for_oa_after_patch: true
a039_has_mapBackend_lua_skip_before_patch: false
a039_mapBackend_skip_not_needed_because_lua_maps_excluded_by_structure: true (current monolith entrypoints.js has no init-side Zig _rebuildMap loop; Lua maps are handled by wrapper registration plus eval/rebuild block, so no a039 skip patch was applied)
a039_has_evalLuaMapData_at_init_before_patch: false
a039_has_callGlobal_rebuildLuaMaps_at_init_before_patch: false
a039_has_evalLuaMapData_at_init_after_patch: true
a039_has_callGlobal_rebuildLuaMaps_at_init_after_patch: true
a039_evalLuaMapData_escaping_matches_monolith: true

a040_has_evalLuaMapData_in_tick_before_patch: false
a040_code_path_structure_matches_monolith: false (pre-patch atom collapsed `hasDynStyles` and `maps||luaMaps` into one branch instead of the three-branch shape documented by the contract and a038)
a040_has_evalLuaMapData_in_tick_after_patch: true

a041_export_symbols_match_monolith: false (atom exports nullable init/tick pointers, returns title as `[*:0]const u8`, and emits JS exports before Lua exports; current monolith entrypoints.js emits a different export surface/order)
a042_main_scaffold_matches_monolith: true
a043_section_patterns_match_monolith: true
a043_section_grouping_matches_monolith: true
a044_pub_promotion_rules_match_monolith: true
a044_cross_reference_prefixing_matches_monolith: true
a045_mkHeader_import_logic_matches_monolith: true
a045_origin_tags_match_monolith: true
a045_split_encoding_matches_monolith: true

a046_calling_convention_differs_from_monolith: true
a046_calling_convention_compatible_with_runEmitAtoms: false
a046_debug_appendix_matches_monolith: true
a046_undefined_zeroing_matches_monolith: true
a046_split_handoff_deferred_to_orchestration: true
a046_split_deferral_is_intentional_and_correct: true (atoms 43-45 own split decomposition; atom 46 correctly leaves the handoff to outer orchestration even though the live monolith finalize path still performs it internally)

Parity artifact: `tsz/compiler/migration/reports/parity/entry_smoke.json`
entry_smoke schema fields present: true
entry_smoke predicted_atoms includes 39 and 40: false
entry_smoke lane: mixed
entry_smoke legacy_hash: 2de77e1f93384606b1c204840f1961e55a6bdf096b4006cdb510eb6c1352e2bb
entry_smoke atom_hash: 8d17d52b4d4a782d09004794aeaf4d1ec280d4d30fb674cfce24e40de9ddd67b
entry_smoke split_output: true
entry_smoke backend_tags: lua_map, nested_map, handlers
entry_smoke legacy_files: app.zig, handlers.zig, logic.zig, maps.zig, nodes.zig, state.zig
entry_smoke atom_error: null
entry_smoke diff_status: DIFF
/home/siah/creative/reactjit/tsz/carts/conformance/mixed/d01_nested_maps.tsz first_diff_hunk:
--- legacy line 1 ---
> const std = @import("std");
  const build_options = @import("build_options");
  const IS_LIB = if (@hasDecl(build_options, "is_lib")) build_options.is_lib else false;
+++ atom line 1 +++
> //! Generated by tsz compiler (Zig) — do not edit
  //!
  //! Source: d01_nested_maps.tsz
Byte parity gate: blocked
Reason: after invalidating prior hashes and rerunning with harness commit 0471f002 in pre-split mode, both paths are real again, but the first byte diff is still at line 1 on the preamble/header surface rather than isolated entry/split ownership.

Split verification: PASS on tsz/carts/conformance/mixed/d01_nested_maps.tsz
split modules produced: true
split module compile status: true (native build reran successfully from /tmp/tsz-gen/generated_d01_nested_maps/app.zig)
split produced files: app.zig, children_manifest.json, handlers.zig, logic.zig, maps.zig, nodes.zig, state.zig

Visual verification gate: blocked
Reason: no visual capture harness exists under tsz/compiler/migration/harness/, so ES087 requires a blocker append and immediate stop before visual capture/finalization steps.

Section-level diff inspection beyond preamble:
- Parity artifact taint: the fresh `entry_smoke.json` run was invoked for `/home/siah/creative/reactjit/tsz/carts/conformance/mixed/d01_nested_maps.tsz`, but both `/tmp/tsz-gen/parity_legacy_presplit.zig` and `/tmp/tsz-gen/parity_atom_output.zig` contain `d102_effect_color_indexing` source/title content instead. This means the byte-parity capture is not isolated to the assigned entry/split cart.
- a039/a040 surface divergence in captured outputs: legacy pre-split emits `luajit_runtime.setMapWrapper(0, @ptrCast(&_root)); state.markDirty();` plus `_first_render` / `luajit_runtime.callGlobal("__render")`, while atom output emits only `_initState();` and an empty `_appTick(now)` body. This is a real section-level mismatch in the entry ownership area, but it is observed on the wrong captured cart payload.
- a046 / split-handoff divergence in captured outputs: atom output still appends `[object Object][object Object]__SPLIT_OUTPUT__` plus empty `__FILE:*__` blocks in what the harness labels as pre-split output. That payload must not be present in a pre-split byte-parity surface and points to finalize/split orchestration leakage.

Latest rerun with forge 39bae560:
- Parity artifact taint persists, but now both captured outputs are for `d96_const_array_map.tsz`, not the assigned `d01_nested_maps.tsz`. Findings below are therefore real section diffs on the wrong cart payload.
- a039 mismatch: legacy init export wraps init in `app_get_init() *const fn () void` and registers wrapper 0 at `_arr_2[3]`; atom emits standalone `fn _appInit()`, returns nullable `_appInit`, and registers wrapper 0 at `nodes._arr_2[2]`. That is a concrete wrapper-node index mismatch plus calling-surface mismatch in owned init output.
- a040 mismatch: legacy tick export wraps `_dirtyTick()` behind `app_get_tick() *const fn () void`, while atom emits inline dirty-tick logic directly inside `fn _appTick(now: u32) void` and exports nullable `?*const fn (u32) void`. The tick ABI and body shape still diverge materially.
- a041 mismatch: legacy export surface includes non-null init/tick getters, Lua-before-JS export order, and `app_get_title_len`; atom still emits nullable init/tick getters, JS-before-Lua order, and omits `app_get_title_len`.
- a046 mismatch: atom output still leaks `[object Object][object Object]__SPLIT_OUTPUT__` and empty split files into the supposed pre-split artifact, proving finalize/split handoff is still wired incorrectly in the atom parity path.

Patch pass after supervisor directive:
- a039 patched: wrapper index calculation now uses the monolith-style `. {` element count minus one and emits pre-split pointers without the `nodes.` namespace prefix.
- a040 patched: emits `_appTickBody()`, `_appTick(now: u32)`, and `_appTickExport()` so engine tick and export tick can diverge cleanly; also restores runtime `qjs_runtime.tick()` / `luajit_runtime.tick()` dispatch when `meta.hasScriptRuntime`.
- a041 patched: export order now matches legacy (`lua` before `js`), `app_get_title_len` is emitted, and exported init/tick getters are non-null (`*const fn () void`) with tick routed through `_appTickExport`.
- a046 patched: parity mode now strips any accidental `__SPLIT_OUTPUT__` payload and `[object Object][object Object]` prefix from the compared atom stream.

Verification after patch pass:
- Forge rebuild completed successfully (`zig build forge`).
- Local atom file re-open checks confirm the source patches are present in a039/a040/a041/a046.
- Parity harness first diff moved off the preamble to legacy line 31 (state-manifest banner width), which indicates the earlier line-1 preamble mismatch was reduced.
- However, the next direct artifact read no longer matched the requested cart: `/tmp/tsz-gen/parity_legacy_presplit.zig` and `/tmp/tsz-gen/parity_atom_output.zig` both showed `counter.tsz`, and the atom artifact still contained nullable exports plus `__SPLIT_OUTPUT__` leakage. That means the parity capture itself remains tainted/stale and cannot yet confirm whether the new source patches closed the owned diffs on `d01_nested_maps`.

Fresh rerun after cross-cart contamination fix (commit 48b41e97):
- `entry_smoke.json` now captures the assigned cart correctly: `/home/siah/creative/reactjit/tsz/carts/conformance/mixed/d01_nested_maps.tsz`.
- Fresh cart-specific hashes: `legacy_hash=d9e452aff2eb3b8b1c9c0c62b8155c6e465824a41de9a7eb065c56af7ecbdc7f`, `atom_hash=4eb9d341c063d483c8319b4929a746d25840ee7f1b59114c6fbab37ff3cf9405`, `diff_status=DIFF`.
- First file-level diff remains outside this contract's owned section: legacy line 31 vs atom line 31 is only the state-manifest banner width.
- a046 verification on the correct cart: PASS. `/tmp/tsz-gen/parity_atom_d01_nested_maps.zig` contains no `__SPLIT_OUTPUT__` marker and no `[object Object]` prefix, so the pre-split leak is no longer present.
- a039 targeted wrapper fix on the correct cart: PASS. Legacy and atom both emit `luajit_runtime.setMapWrapper(1, @ptrCast(&_arr_2[0]));`.
- a041 targeted export-surface fixes on the correct cart: PASS for the requested items. Atom now emits non-null `app_get_init`, non-null `app_get_tick`, Lua exports before JS exports, and `app_get_title_len()`.
- a040 targeted Lua refresh fix on the correct cart: PASS for the contract gap. Atom tick path now emits `qjs_runtime.evalLuaMapData(1, "groups");` before `luajit_runtime.callGlobal("__rebuildLuaMaps");`.

Remaining owned-section diffs on the correct cart:
- a039 still diverges structurally from the monolith pre-split surface. Legacy emits `export fn app_get_init() *const fn () void { const _init = struct { fn init() void { ... } }; return &_init.init; }`, while atom still emits standalone `fn _appInit() void { ... }` and a separate export in a041. In this cart, atom init also still emits `_initState();`, `qjs_runtime.registerHostFn("__setObjArr0", ...)`, `_rebuildMap0();`, and `_updateDynamicText();` where the current monolith init surface does not.
- a040 still diverges structurally from the monolith pre-split surface. Legacy exports a no-arg wrapper returning `_tick.tick` and routes state changes through `_dirtyTick()`, while atom still emits `_appTickBody()`, `_appTick(now: u32)`, and `_appTickExport()` with the dirty logic inlined there.
- Non-owned full-file diffs still remain before the entry section. The atom pre-split file still differs from legacy in state-manifest formatting, `_initState()` placement, zeroed dynamic buffer initialization, and the larger object-array realization block. Those are outside this contract's owned atom set but keep whole-file byte parity at `DIFF`.

Current gate summary after the clean cart-specific rerun:
- contract parity: partial PASS (targeted a039/a040 bridge gaps closed; a046 split leak closed; a039/a040 structural surfaces still differ)
- byte parity: blocked (`DIFF` on correct cart, with owned a039/a040 structural divergence and unrelated earlier-section diffs)
- split smoke: PASS (`./scripts/build carts/conformance/mixed/d01_nested_maps.tsz`)
- visual parity: blocked (no entry/split visual capture harness or captured report data yet)

Latest owned-fix reruns:
- a040 patch rerun: `entry_smoke.json` refreshed to `atom_hash=6bdc5d5b8f466be7c1d4056f4092c39c198992ad678755f3cc0425744831ceb5`, and the correct-cart atom output now includes `_ = _pool_arena.reset(.retain_capacity);` inside `_appTick(now: u32)`.
- a041 patch rerun: `entry_smoke.json` refreshed again to `atom_hash=95c87a905ab84dc24d7a8c481bc98960cb27eb4855a1aec8c2bfd1a787a8ff32`, and the correct-cart atom output now emits monolith-shaped `app_get_init()` / `app_get_tick()` wrappers in the correct order before `app_get_root()`.
- a041 export verification on the correct cart: PASS for the requested ownership items. `app_get_init`, `app_get_tick`, Lua-before-JS ordering, and `app_get_title_len` are all present in the atom parity artifact and match the monolith export wrapper shape.
- a040 tick ABI verification on the correct cart: PASS for the requested ownership item. The exported ABI is again `app_get_tick() *const fn () void`, and the wrapper body now matches the monolith dirty-gate shape (`_dirtyTick(); state.clearDirty(); qjs_runtime.tick(); luajit_runtime.tick();`).

Remaining owned drift after the latest fixes:
- a039 still emits a standalone `fn _appInit() void { ... }` helper for the split/main path. The export wrapper now matches the monolith, but the extra helper still keeps the app section non-identical in the atom pre-split artifact.
- a040 still emits a standalone `fn _appTick(now: u32) void { ... }` helper for the engine main path. The export wrapper now matches the monolith, but the helper remains in the atom app section and keeps byte parity at `DIFF`.
- a046 remains verified fixed on the correct cart: no `__SPLIT_OUTPUT__` marker leakage is present in `/tmp/tsz-gen/parity_atom_d01_nested_maps.zig`.

Final reruns after parity-mode helper suppression:
- a039, a040, and a042 now suppress split-only helper/main emission under `globalThis.__parityMode`, so the cart-specific pre-split atom artifact no longer contains `fn _appInit()`, `fn _appTick()`, or `pub fn main()`.
- a041 now suppresses parity-only state accessor tail emission in `globalThis.__parityMode`, so the cart-specific pre-split atom artifact ends at `export fn app_state_count() usize { return 1; }` like legacy.
- parity intercept now applies a046 manually after atoms 1-45, so cart-specific parity output no longer carries `__SPLIT_OUTPUT__` or `[object Object]` leakage.
- Fresh parity result after those fixes: `legacy_hash=d9e452aff2eb3b8b1c9c0c62b8155c6e465824a41de9a7eb065c56af7ecbdc7f`, `atom_hash=afe5e717e3c488e641b0f625d31179f66be3280e36e66e418e48689596150395`, `diff_status=DIFF`, `verification_time=2026-04-09T09:58:03.265Z`.
- The first remaining whole-file diff is now at legacy line 43 vs atom line 43 in the OA bridge section (`var _oa0_name: [4096][256]u8 = undefined;` vs `const qjs = if (IS_LIB) struct {`), which is outside entry/split/finalize ownership.
- Current cart-specific entry/export slice status: `app_get_init`, `app_get_tick`, `app_get_title_len`, wrapper registration, Lua eval/rebuild ordering, and split-marker hygiene all match the intended legacy pre-split surface for this contract's owned area.
