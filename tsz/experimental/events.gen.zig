//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/experimental/events.tsz
//! Generated by: tsz compile-runtime (imperative mode)
//!
//! To regenerate: tsz compile-runtime tsz/experimental/events.tsz
//! To modify: edit events.tsz and recompile

const std = @import("std");
// ── Functions ──────────────────────────────────────

pub fn hit_test(node: *Node, mx: f32, my: f32) ?Node {
    if (node.style.display == .none) {
        return null;
    }
    var i = node.children.len;
    while (i > 0) {
        i -= 1;
        const hit = hit_test(node.children[@intCast(i)], mx, my);
        if (hit != null) {
            return hit;
        }
    }
    if (has_handlers(node.handlers) or node.input_id != null or node.canvas_type != null) {
        const r = node.computed;
        if (mx >= r.x and mx < r.x + r.w and my >= r.y and my < r.y + r.h) {
            return node;
        }
    }
    return null;
}

fn has_handlers(h: *EventHandler) bool {
    return h.on_press != null or h.on_hover_enter != null or h.on_hover_exit != null or h.on_key != null or h.on_change_text != null or h.on_scroll != null or h.on_right_click != null;
}

pub fn hit_test_text(node: *Node, mx: f32, my: f32) ?Node {
    if (node.style.display == .none) {
        return null;
    }
    var i = node.children.len;
    while (i > 0) {
        i -= 1;
        const hit = hit_test_text(node.children[@intCast(i)], mx, my);
        if (hit != null) {
            return hit;
        }
    }
    if (node.text != null) {
        const r = node.computed;
        if (mx >= r.x and mx < r.x + r.w and my >= r.y and my < r.y + r.h) {
            return node;
        }
    }
    return null;
}

pub fn find_scroll_container(node: *Node, mx: f32, my: f32) ?Node {
    if (node.style.display == .none) {
        return null;
    }
    const r = node.computed;
    if (mx < r.x or mx >= r.x + r.w or my < r.y or my >= r.y + r.h) {
        return null;
    }
    var i = node.children.len;
    while (i > 0) {
        i -= 1;
        const hit = find_scroll_container(node.children[@intCast(i)], mx, my);
        if (hit != null) {
            return hit;
        }
    }
    if (node.style.overflow == .scroll) {
        return node;
    }
    return null;
}
