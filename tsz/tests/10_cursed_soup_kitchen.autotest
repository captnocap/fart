# 10_cursed_soup_kitchen: Every React anti-pattern simultaneously
# Verifies title, HOC stack, recursive dashboard, suspense trap, chaos button

# Title
expect "The React Soup Kitchen"

# Render/mount/count stats
expect "renders:"
expect "mounts:"
expect "count:"

# HOC inception stack
expect "HOC INCEPTION STACK (5 wrappers, new type each render)"
expect "withEvil"
expect "withMoreEvil"
expect "EvilHOC"
expect "TheUnholyBowl"

# Volatile key boundary
expect "VOLATILE KEY:"

# Inline components
expect "InlineItem x3 (defined inside render, new type every time)"
expect "remounts every render"
expect "0"
expect "1"
expect "2"

# Recursive dashboard
expect "RecursiveDashboard"
expect "Depth 0:"
expect "Depth 1:"
expect "Depth 2:"
expect "Depth 3:"
expect "Depth 4:"
expect "admin"
expect "CRITICAL"

# Suspense trap
expect "Suspense Trap (manual throw during render)"
expect "Loading (or crashing)..."
expect "Resolve"

# Eval in JSX
expect "eval() in JSX:"
expect "12:00:00 AM"

# Chaos button
expect "Trigger Chaos (0)"

# Evil bus log
expect "no events yet"

# Anti-patterns footer
expect "Anti-patterns active:"
expect "Proxy re-render on read"
expect "eval() in JSX"

# Resolve suspense
click "Resolve"
expect "The Forbidden Data"
expect "Re-throw (invalidate cache)"

# Toggle recursion bailout
click "Math.random() > 0.1"
expect "bailed out"
reject "CRITICAL"

# Trigger chaos
click "Trigger Chaos (0)"
expect "Trigger Chaos (1)"
