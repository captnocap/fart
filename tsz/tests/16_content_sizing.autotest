# 16_content_sizing: WPT content-based sizing
# Tests: items size to content without explicit width, empty containers visible with padding

# Verify title and description
expect "WPT: content-based sizing"
expect "VERIFY: Items without explicit width size to content, empty containers visible with padding"

# Verify section labels
expect "No explicit width — items size to content"
expect "No explicit height — row items shrink-wrap vertically"
expect "Empty containers — should still be visible with padding"
expect "Mixed content + fixed — grow fills remaining"

# Verify content text inside items
expect "Short"
expect "Medium length"
expect "Longer text content"
expect "A"
expect "B"
expect "Content"
expect "Grows"
expect "80px"
