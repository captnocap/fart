# 70_flex_interaction_stress: auto-generated from runtime snapshot
# Generated by ZIGOS_WITNESS=snapshot — do not edit by hand

# Verify all visible text on initial render
# health: 39 nodes, 13 text, 0 pressable, 0 state slots, 0 fails

# Initial render
expect "WPT: flex property interaction stress"
expect "VERIFY: grow+shrink+basis+min+max all interact, nested grow chai"
expect "grow + shrink + basis + min + max — all at once"
expect "g1 s1 b100 min60 max150"
expect "g2 s0 b50"
expect "justify + align + gap + padding + margin — all at once"
expect "Wrap + grow + shrink + gap + justify:center"
expect "Column: fixed + grow + % + aspect-ratio mixed"
expect "fixed 30px"
expect "20%"
expect "grow"
expect "fixed 40px"
expect "Deeply nested grow chains"
