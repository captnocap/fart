# Cursor IDE — Input & Agent Integration Test
# Exercises: type, key, focus, clear + agent flow + file switching

# === BASELINE ===
expect "Agent"
expect "Sidebar.tsx"
expect "Send"
expect "Ask"

# === MODE SWITCHING — verify pills work ===
click "Task"
expect "Task mode"
expect "Run Task"

click "Ask"
reject "Task mode"
expect "Send"

# === FILE SWITCHING ===
click "Editor.tsx"
expect "Editor Component"

click "package.json"
expect "0.1.0"

click "README.md"
expect "Agentic coding environment"

# Back to Sidebar
click "Sidebar.tsx"
expect "FileTree"

# === TERMINAL TOGGLE ===
click "Terminal"
expect "bash"

# === TOOL TOGGLES ===
styles before "Web"
click "Web"
styles after "Web"

styles before "Term"
click "Term"
styles after "Term"

# === ATTACHMENT ===
click "Attach"
expect "Clear all"

# === NEW CONVERSATION ===
expect "I have analyzed the codebase"
click "+ New"
reject "I have analyzed the codebase"

# === AGENT MODE (after + New, fewer Agent matches) ===
click "Agent" #3
expect "Launch"
click "Ask"
expect "Send"
