# Cursor IDE — landing, workspace chrome, editor, and agent shell

# === BASELINE SHELL ===
expect "Project landing"
expect "reactjit"
expect "EXPLORER"
expect "OPEN EDITORS"
expect "SOURCE CONTROL  0"
expect "Agent Console"
expect "Index Workspace"
expect "Open cursor-ide cart"
expect "Projects"
expect "Git Connections"
expect "Recent Focus"
expect "Settings"
expect "main"
expect "Ln 1"
expect "Workspace"
expect "Send"
reject "nil"

# === VISUAL REGRESSION CHECK ===
color "EXPLORER" #778395

# === OLD MOCK UI MUST STAY GONE ===
reject "src/components/Sidebar.tsx"
reject "Cursor Agent"
reject "I have analyzed the codebase"
reject "TypeScript TSX"
reject "Copilot"

# === AGENT CONTROLS ===
expect "Ask"
expect "Plan"
expect "Task"
expect "Agent"
expect "Web"
expect "Term"
expect "Auto"
expect "Opus 4"
expect "New"
expect "Index"

click "Plan"
expect "Plan mode"
reject "Task mode"
reject "Run Task"
reject "nil"

click "Task"
expect "Task mode"
expect "Run Task"
reject "Plan mode"
reject "nil"

click "Ask"
expect "Send"
reject "Run Task"
reject "Plan mode"
reject "nil"

click "Opus 4"
expect "Sonnet 4"
reject "Opus 4"
reject "nil"

# === TERMINAL PANEL TOGGLE ===
reject "pty attached to workspace shell"
click "Terminal" #1
expect "pty attached to workspace shell"
click "Terminal" #1
reject "pty attached to workspace shell"

# === SEARCH PANEL TOGGLE ===
click "Search" #1
expect "Project Search"
expect "repo"
expect "case"
expect "regex"
click "Search" #1
reject "Project Search"

# === OPEN REAL FILE FROM LANDING ===
click "Open cursor-ide cart"
expect "cursor-ide.tsz"
expect "native view"
expect "Scratch Buffer"
expect "Quiet input, no giant focus frame"
expect "TSZ"
reject "nil"

# === RETURN TO LANDING ===
click "Project landing"
expect "PROJECT LANDING"
expect "Index Workspace"
expect "Open cursor-ide cart"

# === SETTINGS SURFACE ===
click "Settings"
expect "SETTINGS SURFACE"
expect "Model Providers"
expect "Provider routing, context layers, memory, and plugin runtimes"
expect "Compiler Stress Map"
expect "Anthropic"
expect "OpenAI"
reject "nil"

click "Plugins"
expect "Plugin Runtimes"
expect "Lua Runtime"
expect "QJS Runtime"
reject "nil"

click "Capabilities"
expect "SQLite Memory Stores"
expect "File Drop Intake"
expect "Formatting / Syntax Surfaces"
reject "nil"

# === CHAT HEADER ACTIONS ===
click "New"
expect "Send"
reject "nil"
