# d106_slot_prop_branch_shell: Slot props with ternary branches and mapped content
# Tests: slot props with ternary JSX trees containing maps and handlers

# Verify title
expect "Slot Prop Branch Shell"
expect "0 shell actions"

# Verify hero section (Overview mode default)
expect "All Teams"
expect "Core"
expect "Growth"
expect "Infra"
expect "Insights"

# Verify metrics
expect "Visible"
expect "Pinned"
expect "Urgent"

# Verify sidebar cards (default All Teams filter)
expect "Retry queue is saturating"
expect "Finish handoff notes for launch"
expect "Badge spacing regressed again"
expect "Signer rotation is overdue"
expect "Docs sidebar is still too deep"
expect "Need copy pass on empty states"

# Verify unread counts
expect "5 unread"
expect "1 unread"
expect "3 unread"
expect "0 unread"
expect "2 unread"
expect "4 unread"

# Verify footer placeholder
expect "Pick a card to see footer actions."

# Test selecting a card
click "Retry queue is saturating"
expect "1 shell actions"

# Now footer shows actions
expect "Open"
expect "Pin"
expect "Escalate"

# Test team filters
click "Core"
expect "Badge spacing regressed again"
expect "Docs sidebar is still too deep"
reject "Retry queue is saturating"

click "Growth"
expect "Finish handoff notes for launch"
expect "Need copy pass on empty states"

click "Infra"
expect "Retry queue is saturating"
expect "Signer rotation is overdue"

# Switch to Insights mode
click "Insights"

# Verify Insights mode shows pinned cards
expect "Retry queue is saturating"
expect "Signer rotation is overdue"
expect "Back to Overview"

# Test pin action
click "Back to Overview"
click "Retry queue is saturating"
click "Pin"
expect "2 shell actions"

# Test severity indicators
expect "urgent"
expect "watch"
expect "quiet"

# Verify team labels on cards
expect "core"
expect "growth"
expect "infra"

# Verify pinned/unread labels
expect "pinned"
