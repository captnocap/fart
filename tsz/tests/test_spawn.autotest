# test_spawn: auto-generated from runtime snapshot
# Generated by ZIGOS_WITNESS=snapshot — do not edit by hand

# Verify all visible text on initial render
# health: 19 nodes, 10 text, 5 pressable, 0 state slots, 0 fails

# Initial render
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"

# Click-through: 5 pressable nodes

click "press:testSpawnSelf"
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"

click "press:testExecEcho"
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"

click "press:testExecBg"
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"

click "press:testInspectPaths"
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"

click "press:testExecFork"
expect "Process Spawn Test"
expect "Tests whether we can spawn child processes from a running app"
expect "1. spawn_self()"
expect "2. exec echo"
expect "3. exec bg"
expect "4. inspect paths"
expect "5. exec fork"
expect "Log (2 entries)"
expect "Spawn test cart loaded"
expect "Press buttons to test each spawn method"
